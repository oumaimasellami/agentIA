[
  {
    "microservice": "mesx-leveling-board-ui",
    "chemin": "/src/modules/leveling-board/components/Gantt/provider/provideConfig.ts",
    "extension": ".ts",
    "prioritaire": true,
    "contenu": "import { inject } from 'vue';\nimport { CONFIG_KEY } from './symbols.js';\n\n/**\n *\n */\nexport default function provideConfig() {\n  const config = inject(CONFIG_KEY);\n  if (!config) {\n    throw new Error('Failed to inject config!');\n  }\n  return config;\n}\n",
    "taille": 253
  },
  {
    "microservice": "mesx-leveling-board-ui",
    "chemin": "/.azure-pipelines/policies/sonarqube.yml",
    "extension": ".yml",
    "prioritaire": false,
    "contenu": "name: '$(Date:yyyyMMdd)$(Rev:.r)'\n\n# Use the Azure DevOps pipeline templates\nresources:\n  repositories:\n    - repository: 'pipelines'\n      type: 'git'\n      name: 'Azure/template-pipelines'\n\ntrigger:\n  batch: true\n  branches:\n    include:\n      - develop\n      - master\n      - main\n\njobs:\n  - template: jobs/sonar-process.yml@pipelines\n    parameters:\n      agent_pool: ubuntu-2204-vmss-mesx\n      prepare_scanner_mode: CLI\n      prepare_sonarExclusions: $(Build.SourcesDirectory)/node_modules/**\n      prepare_sonarAdditionalProperties: |\n        sonar.sources=$(Build.SourcesDirectory)/src\n        sonar.test.inclusions=**/*.spec.ts\n        sonar.tests=$(Build.SourcesDirectory)/src\n        sonar.typescript.tsconfigPath=$(Build.SourcesDirectory)/tsconfig.lint.json\n\n      step_postPrepare:\n        - template: steps/angular-lint-and-test.yaml@pipelines\n          parameters:\n            nodejs_version: 22.x\n            run_unit_tests: false\n\n        - template: steps/sonarqube-reports.yml@pipelines\n          parameters:\n            reports:\n              - name: sonar.junit.reportPaths\n                path: $(Build.SourcesDirectory)/coverage/junit-tests.xml\n              - name: sonar.eslint.reportPaths\n                path: $(Build.SourcesDirectory)/report.json\n              - name: sonar.testExecutionReportPaths\n                path: $(Build.SourcesDirectory)/coverage/sonar-tests.xml\n              - name: sonar.flex.cobertura.reportPaths\n                path: $(Build.SourcesDirectory)/coverage/cobertura-coverage.xml\n              - name: sonar.javascript.lcov.reportPaths\n                path: $(Build.SourcesDirectory)/coverage/lcov.info\n              - name: sonar.css.stylelint.reportPaths\n                path: $(Build.SourcesDirectory)/stylelint.json\n\n",
    "taille": 1777
  },
  {
    "microservice": "mesx-leveling-board-ui",
    "chemin": "/azure-pipelines.yml",
    "extension": ".yml",
    "prioritaire": false,
    "contenu": "resources:\n  repositories:\n  - repository: mesx-ops-pipelines\n    type: git\n    name: MES_X.0/mesx-ops-pipelines\n\ntrigger:\n  tags:\n    include:\n    - '*'\n  branches:\n    include:\n    - main\n    - ci/*\n  paths:\n    exclude:\n    - README.md\n\nparameters:\n- name: isDebugBuild\n  displayName: 'Build in DEBUG Mode'\n  type: boolean\n  default: false\n\nvariables:\n  isMain: $[eq(variables['Build.SourceBranch'], 'refs/heads/main')]\n  caAppName: lvl-ui\n\nstages:\n- stage: Build\n  jobs:\n  - template: test/jobs/jobs-node-static-tests.yml@mesx-ops-pipelines\n    parameters:\n      folder: $(System.DefaultWorkingDirectory)\n      auditLevel: 'critical'\n  - template: build/jobs/jobs-docker-build.yml@mesx-ops-pipelines\n    parameters:\n      folder: $(System.DefaultWorkingDirectory)\n      debugBuild: ${{ parameters.isDebugBuild }}\n      repository: mesx/leveling-board-ui\n\n- stage: Dev\n  condition: and(succeeded(), eq(variables.isMain, true))\n  jobs:\n  - template: deploy/jobs/jobs-container-apps-deploy.yml@mesx-ops-pipelines\n    parameters:\n      azureSubscription: spn-dev-inf-mes\n      pipelineDeploymentEnvironment: app_cloud_dev_mes\n      containerAppName: ca-gptmpxn01-eun-dev-$(caAppName)\n      containerAppResourceGroupName: rg-gptmpxn01-dev-eun-mes-01\n\n",
    "taille": 1250
  },
  {
    "microservice": "mesx-leveling-board-ui",
    "chemin": "/src/Auth/configuration.ts",
    "extension": ".ts",
    "prioritaire": false,
    "contenu": "import appLogger from '../utils/logger/app-logger.ts';\n\nclass Configuration {\n  public getClientId(): string {\n    const clientId = import.meta.env.IDENTITY_PROVIDER_APP_CLIENT_ID;\n    if (!clientId) {\n      throw new Error('CLIENT_ID not found in environment variables.');\n    }\n    appLogger.debug(this);\n    return clientId;\n  }\n\n  public getAuthority(): string {\n    const authority = import.meta.env.IDENTITY_PROVIDER_APP_AUTHORITY;\n    if (!authority) {\n      throw new Error('AUTHORITY not found in environment variables.');\n    }\n    appLogger.debug(this);\n    return authority;\n  }\n\n  public getRedirectUri(): string {\n    const redirectUri = import.meta.env.APP_REDIRECT_URI;\n    if (!redirectUri) {\n      throw new Error('REDIRECT_URI not found in environment variables.');\n    }\n    appLogger.debug(this);\n    return redirectUri;\n  }\n\n  public getScopes(): string {\n    const scopes = import.meta.env.IDENTITY_PROVIDER_APP_SCOPES;\n    if (!scopes) {\n      throw new Error('SCOPES not found in environment variables.');\n    }\n    appLogger.debug(this);\n    return scopes;\n  }\n\n  public getResponseType(): string {\n    appLogger.debug(this);\n    const code = import.meta.env.IDENTITY_PROVIDER_APP_RESPONSE_TYPE;\n    return code;\n  }\n\n  public getAccessTokenExpiringNotificationTime(): number {\n    appLogger.debug(this);\n    const value =\n      import.meta.env.ACCESS_TOKEN_EXPIRING_REFRESH_SECONDS || '1800';\n    return Number.parseInt(value, 10);\n  }\n}\n\nconst configuration = new Configuration();\n\nexport { configuration };\n",
    "taille": 1536
  },
  {
    "microservice": "mesx-leveling-board-ui",
    "chemin": "/src/Auth/user-service.ts",
    "extension": ".ts",
    "prioritaire": false,
    "contenu": "import axios, { AxiosResponse } from 'axios';\nimport { UserProfile } from '../types/user-profile.dto';\n\nconst umeBackend = import.meta.env.URL_UME_BACKEND;\nconst token = import.meta.env.TOKEN;\ninterface Claim {\n  typ: string;\n  val: string | Array<string>;\n}\n\ninterface DecodedPrincipal {\n  authType: string;\n  claims: Array<Claim>;\n}\n\nexport interface User {\n  profile: UserProfile;\n  lastLogin: Date;\n}\n\nconst extractGroupNames = async (\n  rawGroups: Array<string>,\n): Promise<Array<string>> => {\n  const regex = /CN=([^,]+)/;\n  return rawGroups.map((group) => {\n    const match = regex.exec(group);\n    return match ? match[1] : group;\n  });\n};\n\nexport const getUser = async (): Promise<User | undefined> => {\n  try {\n    const response: AxiosResponse<DecodedPrincipal> = await axios.get(\n      `${umeBackend}/auth/claims`,\n      {\n        headers: {\n          authorization: `Bearer ${token}`,\n        },\n        withCredentials: true,\n      },\n    );\n\n    const claims = response.data?.claims ?? [];\n\n    const getClaim = (suffix: string): string | undefined =>\n      claims.find((c) => c.typ.toLowerCase().endsWith(suffix.toLowerCase()))\n        ?.val as string;\n\n    const getGroupClaims = (): Array<string> => {\n      return claims\n        .filter((c) => c.typ.toLowerCase().endsWith('groups'))\n        .flatMap((c) => {\n          if (Array.isArray(c.val))\n            return c.val.filter((v): v is string => typeof v === 'string');\n          return typeof c.val === 'string' ? [c.val] : [];\n        });\n    };\n\n    const email = getClaim('emailaddress') ?? '';\n    const givenName = getClaim('givenname') ?? '';\n    const familyName = getClaim('surname') ?? '';\n    const groupRaw = getGroupClaims();\n    const groups = await extractGroupNames(groupRaw);\n    const profile = new UserProfile(email, givenName, familyName, groups);\n    return {\n      profile,\n      lastLogin: new Date(),\n    };\n  } catch (error) {\n    console.error('Failed to fetch user claims:', error);\n    return undefined;\n  }\n};\n",
    "taille": 2010
  },
  {
    "microservice": "mesx-leveling-board-ui",
    "chemin": "/src/data.ts",
    "extension": ".ts",
    "prioritaire": false,
    "contenu": "globalThis.desttype = [\n  { id: 1, value: 'POOL' },\n  { id: 2, value: 'TPA' },\n];\n\nglobalThis.srctype = [\n  { id: 1, value: 'SHOP' },\n  { id: 2, value: 'POOL' },\n];\n\nglobalThis.cstmstatus = [\n  { id: 1, value: 'OPEN' },\n  { id: 2, value: 'PIK_CREATED' },\n  { id: 3, value: 'PIK_LAUNCHED' },\n  { id: 4, value: 'PIK_IN_KITTING' },\n  { id: 5, value: 'PIK_IN_PRODUCTION' },\n  { id: 6, value: 'PIK_CLOSED' },\n  { id: 7, value: 'PALLETIZED' },\n  { id: 8, value: 'PICKED' },\n  { id: 9, value: 'TAKE_FROM_STOCK' },\n  { id: 10, value: 'SFG_PIK_LAUNCHED' },\n  { id: 11, value: 'SFG_PIK_IN_KITTING' },\n  { id: 12, value: 'SFG_PIK_IN_PRODUCTION' },\n  { id: 13, value: 'SFG_PIK_CLOSED' },\n];\n",
    "taille": 679
  },
  {
    "microservice": "mesx-leveling-board-ui",
    "chemin": "/src/fullscreen.d.ts",
    "extension": ".ts",
    "prioritaire": false,
    "contenu": "declare global {\n  interface Document {\n    mozCancelFullScreen?: () => void;\n    mozRequestFullScreen?: () => void;\n    webkitExitFullscreen?: () => void;\n    webkitRequestFullscreen?: () => void;\n    msExitFullscreen?: () => void;\n    msRequestFullscreen?: () => void;\n    mozFullScreenElement?: Element | null;\n    webkitFullscreenElement?: Element | null;\n    msFullscreenElement?: Element | null;\n  }\n\n  interface HTMLElement {\n    mozRequestFullScreen?: () => void; // Firefox-specific method\n    webkitRequestFullscreen?: () => void; // Webkit-based browsers\n    msRequestFullscreen?: () => void; // Internet Explorer / Edge\n  }\n}\n\nexport {};\n",
    "taille": 650
  },
  {
    "microservice": "mesx-leveling-board-ui",
    "chemin": "/src/global-service/store.ts",
    "extension": ".ts",
    "prioritaire": false,
    "contenu": "import { ref, reactive } from 'vue';\n\n/**\n *\n */\nfunction getValueQ(): string {\n  const storedValue = window.localStorage.getItem('VALUEQ');\n  const parsedValue = storedValue ? JSON.parse(storedValue) : true;\n  return parsedValue;\n}\n\n/**\n *\n */\nfunction getValueE(): string {\n  const storedValue = window.localStorage.getItem('VALUEE');\n  const parsedValue = storedValue ? JSON.parse(storedValue) : true;\n  return parsedValue;\n}\n\nconst state = reactive({\n  valueE: ref(JSON.parse(getValueE())),\n  valueQ: ref(JSON.parse(getValueQ())),\n});\n/* eslint @typescript-eslint/no-explicit-any: \"off\" */\n/* eslint @typescript-eslint/explicit-module-boundary-types: \"off\" */\n/**\n *\n * @param value\n */\nfunction setValueQ(value: any): void {\n  state.valueQ = value;\n  window.localStorage.setItem('VALUEQ', JSON.stringify(value));\n}\n\n/**\n *\n * @param value\n */\nfunction setValueE(value: any): void {\n  state.valueE = value;\n  window.localStorage.setItem('VALUEE', JSON.stringify(value));\n}\n\n// Export necessary functions and data\nexport { state, setValueQ, setValueE, getValueQ, getValueE };\n",
    "taille": 1079
  },
  {
    "microservice": "mesx-leveling-board-ui",
    "chemin": "/src/global.d.ts",
    "extension": ".ts",
    "prioritaire": false,
    "contenu": "declare global {\n  var desttype: Array<{ id: number; value: string }>;\n  var srctype: Array<{ id: number; value: string }>;\n  var cstmstatus: Array<{ id: number; value: string }>;\n}\n\nexport {};\n",
    "taille": 194
  },
  {
    "microservice": "mesx-leveling-board-ui",
    "chemin": "/src/localization/i18n.ts",
    "extension": ".ts",
    "prioritaire": false,
    "contenu": "// I18n.ts\nimport { createI18n, I18n } from 'vue-i18n';\nimport { useProfileStore } from '../store/user-auth-store';\nimport en from './locales/en.json';\nimport fr from './locales/fr.json';\n\n/**\n * Setup the i18n instance with the selected locale and available messages.\n * @returns A promise that resolves to the i18n instance.\n */\nexport async function setupI18n(): Promise<I18n> {\n  const userProfile = useProfileStore();\n  const locales = userProfile.userAttributes.locale || 'en';\n  return createI18n({\n    locale: locales,\n    fallbackLocale: 'en',\n    messages: { en, fr },\n    legacy: false,\n    globalInjection: true,\n  });\n}\n",
    "taille": 633
  },
  {
    "microservice": "mesx-leveling-board-ui",
    "chemin": "/src/main.ts",
    "extension": ".ts",
    "prioritaire": false,
    "contenu": "import mainHeader, { NotifyPlugin } from '@mesx/common-ui-lib';\nimport { createPinia } from 'pinia';\nimport piniaPluginPersistedstate from 'pinia-plugin-persistedstate';\nimport { Quasar } from 'quasar';\nimport { createApp } from 'vue';\nimport App from './App.vue';\nimport { setupI18n } from './localization/i18n.ts';\nimport ganttastic from './modules/leveling-board/components/Gantt/vue-ganttastic.ts';\nimport quasarConfig from './quasar-user-options.ts';\nimport { router } from './router/index.ts';\nimport './data.ts';\nimport '@mesx/common-ui-lib/style.css';\n\n/**\n *\n */\nasync function initializeApp(): Promise<void> {\n  const pinia = createPinia();\n  const app = createApp(App);\n\n  app.use(Quasar, quasarConfig);\n  app.use(pinia);\n  pinia.use(piniaPluginPersistedstate);\n  app.use(router);\n  app.use(ganttastic);\n  app.use(mainHeader);\n  const { $q } = app.config.globalProperties;\n\n  app.use(NotifyPlugin, { $q });\n\n  try {\n    const i18n = await setupI18n();\n    app.use(i18n);\n  } catch (error) {\n    console.error('Failed to initialize i18n:', error);\n  }\n\n  app.mount('#app');\n}\n\ninitializeApp();\n",
    "taille": 1104
  },
  {
    "microservice": "mesx-leveling-board-ui",
    "chemin": "/src/modules/leveling-board/components/Gantt/color-schemes.ts",
    "extension": ".ts",
    "prioritaire": false,
    "contenu": "import type * as CSS from 'csstype';\n\ntype Color = CSS.DataType.Color;\n\nexport interface ColorScheme {\n  primary: Color;\n  secondary: Color;\n  ternary: Color;\n  quartenary: Color;\n  hoverHighlight: Color;\n  markerCurrentTime: Color;\n  text: Color;\n  background: Color;\n  toast?: Color;\n}\n\nexport const colorSchemes: Record<string, ColorScheme> = {\n  default: {\n    primary: '#eeeeee',\n    secondary: '#E0E0E0',\n    ternary: '#F5F5F5',\n    quartenary: '#ededed',\n    hoverHighlight: 'rgba(204, 216, 219, 0.5)',\n    markerCurrentTime: 'red',\n    text: '#404040',\n    background: 'white',\n  },\n\n  creamy: {\n    primary: '#ffe8d9',\n    secondary: '#fcdcc5',\n    ternary: '#fff6f0',\n    quartenary: '#f7ece6',\n    hoverHighlight: 'rgba(230, 221, 202, 0.5)',\n    markerCurrentTime: '#000',\n    text: '#542d05',\n    background: 'white',\n  },\n\n  crimson: {\n    primary: '#a82039',\n    secondary: '#c41238',\n    ternary: '#db4f56',\n    quartenary: '#ce5f64',\n    hoverHighlight: 'rgba(196, 141, 141, 0.5)',\n    markerCurrentTime: '#000',\n    text: 'white',\n    background: 'white',\n  },\n\n  dark: {\n    primary: '#404040',\n    secondary: '#303030',\n    ternary: '#353535',\n    quartenary: '#383838',\n    hoverHighlight: 'rgba(159, 160, 161, 0.5)',\n    markerCurrentTime: '#fff',\n    text: 'white',\n    background: '#525252',\n    toast: '#1f1f1f',\n  },\n\n  flare: {\n    primary: '#e08a38',\n    secondary: '#e67912',\n    ternary: '#5e5145',\n    quartenary: '#665648',\n    hoverHighlight: 'rgba(196, 141, 141, 0.5)',\n    markerCurrentTime: '#000',\n    text: 'white',\n    background: 'white',\n  },\n\n  fuchsia: {\n    primary: '#de1d5a',\n    secondary: '#b50b41',\n    ternary: '#ff7da6',\n    quartenary: '#f2799f',\n    hoverHighlight: 'rgba(196, 141, 141, 0.5)',\n    markerCurrentTime: '#000',\n    text: 'white',\n    background: 'white',\n  },\n\n  grove: {\n    primary: '#3d9960',\n    secondary: '#288542',\n    ternary: '#72b585',\n    quartenary: '#65a577',\n    hoverHighlight: 'rgba(160, 219, 171, 0.5)',\n    markerCurrentTime: '#000',\n    text: 'white',\n    background: 'white',\n  },\n\n  'material-blue': {\n    primary: '#0D47A1',\n    secondary: '#1565C0',\n    ternary: '#42a5f5',\n    quartenary: '#409fed',\n    hoverHighlight: 'rgba(110, 165, 196, 0.5)',\n    markerCurrentTime: '#000',\n    text: 'white',\n    background: 'white',\n  },\n\n  sky: {\n    primary: '#b5e3ff',\n    secondary: '#a1d6f7',\n    ternary: '#d6f7ff',\n    quartenary: '#d0edf4',\n    hoverHighlight: 'rgba(193, 202, 214, 0.5)',\n    markerCurrentTime: '#000',\n    text: '#022c47',\n    background: 'white',\n  },\n\n  slumber: {\n    primary: '#2a2f42',\n    secondary: '#2f3447',\n    ternary: '#35394d',\n    quartenary: '#2c3044',\n    hoverHighlight: 'rgba(179, 162, 127, 0.5)',\n    markerCurrentTime: '#fff',\n    text: '#ffe0b3',\n    background: '#38383b',\n    toast: '#1f1f1f',\n  },\n\n  vue: {\n    primary: '#258a5d',\n    secondary: '#41B883',\n    ternary: '#35495E',\n    quartenary: '#2a3d51',\n    hoverHighlight: 'rgba(160, 219, 171, 0.5)',\n    markerCurrentTime: '#000',\n    text: 'white',\n    background: 'white',\n  },\n};\n\nexport type ColorSchemeKey = keyof typeof colorSchemes;\n\nexport default colorSchemes;\n",
    "taille": 3157
  },
  {
    "microservice": "mesx-leveling-board-ui",
    "chemin": "/src/modules/leveling-board/components/Gantt/composables/createBarDrag.ts",
    "extension": ".ts",
    "prioritaire": false,
    "contenu": "import { ref } from 'vue';\nimport type { GGanttChartConfig } from '../components/GGanttChart.vue';\nimport provideConfig from '../provider/provideConfig.js';\n\nimport type { GanttBarObject } from '../types';\nimport useDayjsHelper from './useDayjsHelper.js';\nimport useTimePositionMapping from './useTimePositionMapping.js';\n\n/**\n *\n * @param bar\n * @param onDrag\n * @param onEndDrag\n * @param config\n */\nexport default function createBarDrag(\n  bar: GanttBarObject,\n  onDrag: (e: MouseEvent, bar: GanttBarObject) => void = () => null,\n  onEndDrag: (e: MouseEvent, bar: GanttBarObject) => void = () => null,\n  config: GGanttChartConfig = provideConfig(),\n) {\n  const { barStart, barEnd, pushOnOverlap } = config;\n\n  const isDragging = ref(false);\n  let cursorOffsetX = 0;\n  let dragCallBack: (e: MouseEvent) => void;\n\n  const { mapPositionToTime } = useTimePositionMapping(config);\n  const { toDayjs } = useDayjsHelper(config);\n\n  const initDrag = (e: MouseEvent) => {\n    const barElement = document.getElementById(bar.ganttBarConfig.id);\n    if (!barElement) {\n      return;\n    }\n\n    cursorOffsetX = e.clientX - (barElement.getBoundingClientRect().left || 0);\n    const mousedownType = (e.target as Element).className;\n    switch (mousedownType) {\n      case 'g-gantt-bar-handle-left': {\n        document.body.style.cursor = 'ew-resize';\n        dragCallBack = dragByLeftHandle;\n        break;\n      }\n      case 'g-gantt-bar-handle-right': {\n        document.body.style.cursor = 'ew-resize';\n        dragCallBack = dragByRightHandle;\n        break;\n      }\n      default: {\n        dragCallBack = drag;\n      }\n    }\n    isDragging.value = true;\n    window.addEventListener('mousemove', dragCallBack);\n    window.addEventListener('mouseup', endDrag);\n  };\n\n  const getBarElements = () => {\n    const barElement = document.getElementById(bar.ganttBarConfig.id);\n    const barContainer = barElement\n      ?.closest('.g-gantt-row-bars-container')\n      ?.getBoundingClientRect();\n    return { barElement, barContainer };\n  };\n\n  const drag = (e: MouseEvent) => {\n    const { barElement, barContainer } = getBarElements();\n    if (!barElement || !barContainer) {\n      return;\n    }\n\n    const barWidth = barElement.getBoundingClientRect().width;\n    const xStart = e.clientX - barContainer.left - cursorOffsetX;\n    const xEnd = xStart + barWidth;\n    if (isOutOfRange(xStart, xEnd)) {\n      return;\n    }\n    bar[barStart.value] = mapPositionToTime(xStart);\n    bar[barEnd.value] = mapPositionToTime(xEnd);\n    onDrag(e, bar);\n  };\n\n  const dragByLeftHandle = (e: MouseEvent) => {\n    const { barElement, barContainer } = getBarElements();\n    if (!barElement || !barContainer) {\n      return;\n    }\n\n    const xStart = e.clientX - barContainer.left;\n    const newBarStart = mapPositionToTime(xStart);\n    if (toDayjs(newBarStart).isSameOrAfter(toDayjs(bar, 'end'))) {\n      return;\n    }\n    bar[barStart.value] = newBarStart;\n    onDrag(e, bar);\n  };\n\n  const dragByRightHandle = (e: MouseEvent) => {\n    const { barElement, barContainer } = getBarElements();\n    if (!barElement || !barContainer) {\n      return;\n    }\n\n    const xEnd = e.clientX - barContainer.left;\n    const newBarEnd = mapPositionToTime(xEnd);\n    if (toDayjs(newBarEnd).isSameOrBefore(toDayjs(bar, 'start'))) {\n      return;\n    }\n    bar[barEnd.value] = newBarEnd;\n    onDrag(e, bar);\n  };\n\n  const isOutOfRange = (xStart?: number, xEnd?: number) => {\n    if (!pushOnOverlap.value) {\n      return false;\n    }\n    const { dragLimitLeft } = bar.ganttBarConfig;\n    const { dragLimitRight } = bar.ganttBarConfig;\n\n    return (\n      (xStart && dragLimitLeft != undefined && xStart < dragLimitLeft) ||\n      (xEnd && dragLimitRight != undefined && xEnd > dragLimitRight)\n    );\n  };\n\n  const endDrag = (e: MouseEvent) => {\n    isDragging.value = false;\n    document.body.style.cursor = '';\n    window.removeEventListener('mousemove', dragCallBack);\n    window.removeEventListener('mouseup', endDrag);\n    onEndDrag(e, bar);\n  };\n\n  return {\n    isDragging,\n    initDrag,\n  };\n}\n",
    "taille": 4051
  },
  {
    "microservice": "mesx-leveling-board-ui",
    "chemin": "/src/modules/leveling-board/components/Gantt/composables/useBarDragLimit.ts",
    "extension": ".ts",
    "prioritaire": false,
    "contenu": "import provideConfig from '../provider/provideConfig.js';\nimport provideGetChartRows from '../provider/provideGetChartRows.js';\nimport type { GanttBarObject } from '../types';\n\n/**\n *\n */\nexport default function useBarDragLimit() {\n  const { pushOnOverlap } = provideConfig();\n  const getChartRows = provideGetChartRows();\n\n  const getBarsFromBundle = (bundle?: string) => {\n    const res: Array<GanttBarObject> = [];\n    if (bundle != undefined) {\n      for (const row of getChartRows()) {\n        for (const bar of row.bars) {\n          if (bar.ganttBarConfig.bundle === bundle) {\n            res.push(bar);\n          }\n        }\n      }\n    }\n    return res;\n  };\n\n  const setDragLimitsOfGanttBar = (bar: GanttBarObject) => {\n    if (!pushOnOverlap.value || bar.ganttBarConfig.pushOnOverlap === false) {\n      return;\n    }\n    for (const sideValue of ['left', 'right']) {\n      const side = sideValue as 'left' | 'right';\n      const { gapDistanceSoFar, bundleBarsAndGapDist } =\n        countGapDistanceToNextImmobileBar(bar, 0, side);\n      let totalGapDistance = gapDistanceSoFar;\n      const bundleBarsOnPath = bundleBarsAndGapDist;\n      if (!bundleBarsOnPath) {\n        continue;\n      }\n\n      for (let index = 0; index < bundleBarsOnPath.length; index++) {\n        const barFromBundle = bundleBarsOnPath[index].bar;\n        const gapDistribution = bundleBarsOnPath[index].gapDistance;\n        const otherBarsFromBundle = getBarsFromBundle(\n          barFromBundle.ganttBarConfig.bundle,\n        ).filter((otherBar) => otherBar !== barFromBundle);\n        for (const otherBar of otherBarsFromBundle) {\n          const nextGapDistanceAndBars = countGapDistanceToNextImmobileBar(\n            otherBar,\n            gapDistribution,\n            side,\n          );\n          const newGapDistance = nextGapDistanceAndBars.gapDistanceSoFar;\n          const newBundleBars = nextGapDistanceAndBars.bundleBarsAndGapDist;\n          if (\n            newGapDistance != undefined &&\n            (!totalGapDistance || newGapDistance < totalGapDistance)\n          ) {\n            totalGapDistance = newGapDistance;\n          }\n          for (const newBundleBar of newBundleBars) {\n            if (\n              !bundleBarsOnPath.find(\n                (barAndGap) => barAndGap.bar === newBundleBar.bar,\n              )\n            ) {\n              bundleBarsOnPath.push(newBundleBar);\n            }\n          }\n        }\n      }\n      const barElement = document.getElementById(\n        bar.ganttBarConfig.id,\n      ) as HTMLElement;\n      if (totalGapDistance != undefined && side === 'left') {\n        bar.ganttBarConfig.dragLimitLeft =\n          barElement.offsetLeft - totalGapDistance;\n      } else if (totalGapDistance != undefined && side === 'right') {\n        bar.ganttBarConfig.dragLimitRight =\n          barElement.offsetLeft + barElement.offsetWidth + totalGapDistance;\n      }\n    }\n    // All bars from the bundle of the clicked bar need to have the same drag limit:\n    const barsFromBundleOfClickedBar = getBarsFromBundle(\n      bar.ganttBarConfig.bundle,\n    );\n    for (const barFromBundle of barsFromBundleOfClickedBar) {\n      barFromBundle.ganttBarConfig.dragLimitLeft =\n        bar.ganttBarConfig.dragLimitLeft;\n      barFromBundle.ganttBarConfig.dragLimitRight =\n        bar.ganttBarConfig.dragLimitRight;\n    }\n  };\n\n  // Returns the gap distance to the next immobile bar\n  // In the row where the given bar (parameter) is (added to gapDistanceSoFar)\n  // And a list of all bars on that path that belong to a bundle\n  const countGapDistanceToNextImmobileBar = (\n    bar: GanttBarObject,\n    gapDistanceSoFar = 0,\n    side: 'left' | 'right',\n  ) => {\n    const bundleBarsAndGapDistribution = bar.ganttBarConfig.bundle\n      ? [{ bar, gapDistance: gapDistanceSoFar }]\n      : [];\n    let currentBar = bar;\n    let nextBar = getNextGanttBar(currentBar, side);\n    // Left side:\n    if (side === 'left') {\n      while (nextBar) {\n        const currentBarElement = document.getElementById(\n          currentBar.ganttBarConfig.id,\n        ) as HTMLElement;\n        const nextBarElement = document.getElementById(\n          nextBar.ganttBarConfig.id,\n        ) as HTMLElement;\n        const nextBarOffsetRight =\n          nextBarElement.offsetLeft + nextBarElement.offsetWidth;\n        gapDistanceSoFar += currentBarElement.offsetLeft - nextBarOffsetRight;\n        if (nextBar.ganttBarConfig.immobile) {\n          return {\n            gapDistanceSoFar,\n            bundleBarsAndGapDist: bundleBarsAndGapDistribution,\n          };\n        }\n        if (nextBar.ganttBarConfig.bundle) {\n          bundleBarsAndGapDistribution.push({\n            bar: nextBar,\n            gapDistance: gapDistanceSoFar,\n          });\n        }\n        currentBar = nextBar;\n        nextBar = getNextGanttBar(nextBar, 'left');\n      }\n    }\n    if (side === 'right') {\n      while (nextBar) {\n        const currentBarElement = document.getElementById(\n          currentBar.ganttBarConfig.id,\n        ) as HTMLElement;\n        const nextBarElement = document.getElementById(\n          nextBar.ganttBarConfig.id,\n        ) as HTMLElement;\n        const currentBarOffsetRight =\n          currentBarElement.offsetLeft + currentBarElement.offsetWidth;\n        gapDistanceSoFar += nextBarElement.offsetLeft - currentBarOffsetRight;\n        if (nextBar.ganttBarConfig.immobile) {\n          return {\n            gapDistanceSoFar,\n            bundleBarsAndGapDist: bundleBarsAndGapDistribution,\n          };\n        }\n        if (nextBar.ganttBarConfig.bundle) {\n          bundleBarsAndGapDistribution.push({\n            bar: nextBar,\n            gapDistance: gapDistanceSoFar,\n          });\n        }\n        currentBar = nextBar;\n        nextBar = getNextGanttBar(nextBar, 'right');\n      }\n    }\n    return {\n      gapDistanceSoFar: null,\n      bundleBarsAndGapDist: bundleBarsAndGapDistribution,\n    };\n  };\n\n  const getNextGanttBar = (bar: GanttBarObject, side: 'left' | 'right') => {\n    const barElement = document.getElementById(\n      bar.ganttBarConfig.id,\n    ) as HTMLElement;\n    const allBarsInRow =\n      getChartRows().find((row) => row.bars.includes(bar))?.bars ?? [];\n    let allBarsLeftOrRight = [];\n    allBarsLeftOrRight =\n      side === 'left'\n        ? allBarsInRow.filter((otherBar) => {\n            const otherBarElement = document.getElementById(\n              otherBar.ganttBarConfig.id,\n            ) as HTMLElement;\n            return (\n              otherBarElement &&\n              otherBarElement.offsetLeft < barElement.offsetLeft &&\n              otherBar.ganttBarConfig.pushOnOverlap !== false\n            );\n          })\n        : allBarsInRow.filter((otherBar) => {\n            const otherBarElement = document.getElementById(\n              otherBar.ganttBarConfig.id,\n            ) as HTMLElement;\n            return (\n              otherBarElement &&\n              otherBarElement.offsetLeft > barElement.offsetLeft &&\n              otherBar.ganttBarConfig.pushOnOverlap !== false\n            );\n          });\n    if (allBarsLeftOrRight.length > 0) {\n      return allBarsLeftOrRight.reduce((bar1, bar2) => {\n        const bar1Element = document.getElementById(\n          bar1.ganttBarConfig.id,\n        ) as HTMLElement;\n        const bar2Element = document.getElementById(\n          bar2.ganttBarConfig.id,\n        ) as HTMLElement;\n        const bar1Distribution = Math.abs(\n          bar1Element.offsetLeft - barElement.offsetLeft,\n        );\n        const bar2Distribution = Math.abs(\n          bar2Element.offsetLeft - barElement.offsetLeft,\n        );\n        return bar1Distribution < bar2Distribution ? bar1 : bar2;\n      }, allBarsLeftOrRight[0]);\n    }\n    return null;\n  };\n\n  return {\n    setDragLimitsOfGanttBar,\n  };\n}\n",
    "taille": 7754
  },
  {
    "microservice": "mesx-leveling-board-ui",
    "chemin": "/src/modules/leveling-board/components/Gantt/composables/useBarDragManagement.ts",
    "extension": ".ts",
    "prioritaire": false,
    "contenu": "import provideConfig from '../provider/provideConfig.js';\nimport provideEmitBarEvent from '../provider/provideEmitBarEvent.js';\nimport provideGetChartRows from '../provider/provideGetChartRows.js';\nimport type { GanttBarObject } from '../types';\n\nimport createBarDrag from './createBarDrag.js';\nimport useDayjsHelper from './useDayjsHelper.js';\n\n/**\n *\n */\nexport default function useBarDragManagement() {\n  const config = provideConfig();\n  const getChartRows = provideGetChartRows();\n  const emitBarEvent = provideEmitBarEvent();\n  const { pushOnOverlap, barStart, barEnd, noOverlap, dateFormat } = config;\n\n  const movedBarsInDrag = new Map<\n    GanttBarObject,\n    { oldStart: string; oldEnd: string }\n  >();\n\n  const { toDayjs, format } = useDayjsHelper();\n\n  const initDragOfBar = (bar: GanttBarObject, e: MouseEvent) => {\n    const { initDrag } = createBarDrag(bar, onDrag, onEndDrag, config);\n    emitBarEvent({ ...e, type: 'dragstart' }, bar);\n    initDrag(e);\n    addBarToMovedBars(bar);\n  };\n\n  const initDragOfBundle = (mainBar: GanttBarObject, e: MouseEvent) => {\n    const { bundle } = mainBar.ganttBarConfig;\n    if (bundle == undefined) {\n      return;\n    }\n    for (const row of getChartRows()) {\n      for (const bar of row.bars) {\n        if (bar.ganttBarConfig.bundle === bundle) {\n          const dragEndHandler = bar === mainBar ? onEndDrag : () => null;\n          const { initDrag } = createBarDrag(\n            bar,\n            onDrag,\n            dragEndHandler,\n            config,\n          );\n          initDrag(e);\n          addBarToMovedBars(bar);\n        }\n      }\n    }\n    emitBarEvent({ ...e, type: 'dragstart' }, mainBar);\n  };\n\n  const onDrag = (e: MouseEvent, bar: GanttBarObject) => {\n    emitBarEvent({ ...e, type: 'drag' }, bar);\n    fixOverlaps(bar);\n  };\n\n  const fixOverlaps = (ganttBar: GanttBarObject) => {\n    if (!pushOnOverlap?.value) {\n      return;\n    }\n    let currentBar = ganttBar;\n    let { overlapBar, overlapType } = getOverlapBarAndType(currentBar);\n    while (overlapBar) {\n      addBarToMovedBars(overlapBar);\n      const currentBarStart = toDayjs(currentBar[barStart.value]);\n      const currentBarEnd = toDayjs(currentBar[barEnd.value]);\n      const overlapBarStart = toDayjs(overlapBar[barStart.value]);\n      const overlapBarEnd = toDayjs(overlapBar[barEnd.value]);\n      let minuteDiff: number;\n      switch (overlapType) {\n        case 'left': {\n          minuteDiff = overlapBarEnd.diff(currentBarStart, 'minutes', true);\n          overlapBar[barEnd.value] = format(\n            currentBar[barStart.value],\n            dateFormat.value,\n          );\n          overlapBar[barStart.value] = format(\n            overlapBarStart.subtract(minuteDiff, 'minutes'),\n            dateFormat.value,\n          );\n          break;\n        }\n        case 'right': {\n          minuteDiff = currentBarEnd.diff(overlapBarStart, 'minutes', true);\n          overlapBar[barStart.value] = format(currentBarEnd, dateFormat.value);\n          overlapBar[barEnd.value] = format(\n            overlapBarEnd.add(minuteDiff, 'minutes'),\n            dateFormat.value,\n          );\n          break;\n        }\n        default: {\n          console.warn(\n            'Vue-Ganttastic: One bar is inside of the other one! This should never occur while push-on-overlap is active!',\n          );\n          return;\n        }\n      }\n      if (overlapBar && (overlapType === 'left' || overlapType === 'right')) {\n        moveBundleOfPushedBarByMinutes(overlapBar, minuteDiff, overlapType);\n      }\n      currentBar = overlapBar;\n      ({ overlapBar, overlapType } = getOverlapBarAndType(overlapBar));\n    }\n  };\n\n  const getOverlapBarAndType = (ganttBar: GanttBarObject) => {\n    let overlapInBetween;\n    let overlapLeft;\n    let overlapRight;\n    const allBarsInRow =\n      getChartRows().find((row) => row.bars.includes(ganttBar))?.bars ?? [];\n    const ganttBarStart = toDayjs(ganttBar[barStart.value]);\n    const ganttBarEnd = toDayjs(ganttBar[barEnd.value]);\n    const overlapBar = allBarsInRow.find((otherBar) => {\n      if (otherBar === ganttBar) {\n        return false;\n      }\n      const otherBarStart = toDayjs(otherBar[barStart.value]);\n      const otherBarEnd = toDayjs(otherBar[barEnd.value]);\n      overlapLeft = ganttBarStart.isBetween(otherBarStart, otherBarEnd);\n      overlapRight = ganttBarEnd.isBetween(otherBarStart, otherBarEnd);\n      overlapInBetween =\n        otherBarStart.isBetween(ganttBarStart, ganttBarEnd) ||\n        otherBarEnd.isBetween(ganttBarStart, ganttBarEnd);\n      return overlapLeft || overlapRight || overlapInBetween;\n    });\n    const overlapType = overlapLeft\n      ? 'left'\n      : overlapRight\n        ? 'right'\n        : overlapInBetween\n          ? 'between'\n          : null;\n    return { overlapBar, overlapType };\n  };\n\n  const moveBundleOfPushedBarByMinutes = (\n    pushedBar: GanttBarObject,\n    minutes: number,\n    direction: 'left' | 'right',\n  ) => {\n    addBarToMovedBars(pushedBar);\n    if (!pushedBar.ganttBarConfig.bundle) {\n      return;\n    }\n    for (const row of getChartRows()) {\n      for (const bar of row.bars) {\n        if (\n          bar.ganttBarConfig.bundle === pushedBar.ganttBarConfig.bundle &&\n          bar !== pushedBar\n        ) {\n          addBarToMovedBars(bar);\n          moveBarByMinutes(bar, minutes, direction);\n        }\n      }\n    }\n  };\n\n  const moveBarByMinutes = (\n    bar: GanttBarObject,\n    minutes: number,\n    direction: 'left' | 'right',\n  ) => {\n    switch (direction) {\n      case 'left': {\n        bar[barStart.value] = format(\n          toDayjs(bar, 'start').subtract(minutes, 'minutes'),\n          dateFormat.value,\n        );\n        bar[barEnd.value] = format(\n          toDayjs(bar, 'end').subtract(minutes, 'minutes'),\n          dateFormat.value,\n        );\n        break;\n      }\n      case 'right': {\n        bar[barStart.value] = format(\n          toDayjs(bar, 'start').add(minutes, 'minutes'),\n          dateFormat.value,\n        );\n        bar[barEnd.value] = format(\n          toDayjs(bar, 'end').add(minutes, 'minutes'),\n          dateFormat.value,\n        );\n      }\n    }\n    fixOverlaps(bar);\n  };\n\n  const onEndDrag = (e: MouseEvent, bar: GanttBarObject) => {\n    snapBackAllMovedBarsIfNeeded();\n    const event_ = {\n      ...e,\n      type: 'dragend',\n    };\n    emitBarEvent(event_, bar, undefined, new Map(movedBarsInDrag));\n    movedBarsInDrag.clear();\n  };\n\n  const addBarToMovedBars = (bar: GanttBarObject) => {\n    if (!movedBarsInDrag.has(bar)) {\n      const oldStart = bar[barStart.value];\n      const oldEnd = bar[barEnd.value];\n      movedBarsInDrag.set(bar, { oldStart, oldEnd });\n    }\n  };\n\n  const snapBackAllMovedBarsIfNeeded = () => {\n    if (pushOnOverlap.value || !noOverlap.value) {\n      return;\n    }\n\n    let isAnyOverlap = false;\n    for (const [bar, _] of movedBarsInDrag.entries()) {\n      const { overlapBar } = getOverlapBarAndType(bar);\n      if (overlapBar != undefined) {\n        isAnyOverlap = true;\n      }\n    }\n    if (!isAnyOverlap) {\n      return;\n    }\n    for (const [bar, { oldStart, oldEnd }] of movedBarsInDrag.entries()) {\n      bar[barStart.value] = oldStart;\n      bar[barEnd.value] = oldEnd;\n    }\n  };\n\n  return {\n    initDragOfBar,\n    initDragOfBundle,\n  };\n}\n",
    "taille": 7256
  },
  {
    "microservice": "mesx-leveling-board-ui",
    "chemin": "/src/modules/leveling-board/components/Gantt/composables/useDayjsHelper.ts",
    "extension": ".ts",
    "prioritaire": false,
    "contenu": "import dayjs, { type Dayjs } from 'dayjs';\nimport { computed } from 'vue';\n\nimport type { GGanttChartConfig } from '../components/GGanttChart.vue';\nimport provideConfig from '../provider/provideConfig.js';\nimport type { GanttBarObject } from '../types';\n\nexport const DEFAULT_DATE_FORMAT = 'YYYY-MM-DD HH:mm';\n\n/**\n *\n * @param config\n */\nexport default function useDayjsHelper(\n  config: GGanttChartConfig = provideConfig(),\n) {\n  const { chartStart, chartEnd, barStart, barEnd, dateFormat } = config;\n\n  const chartStartDayjs = computed(() => toDayjs(chartStart.value));\n  const chartEndDayjs = computed(() => toDayjs(chartEnd.value));\n\n  const toDayjs = (\n    input: string | Date | GanttBarObject,\n    startOrEnd?: 'start' | 'end',\n  ) => {\n    let value;\n    if (\n      startOrEnd !== undefined &&\n      typeof input !== 'string' &&\n      !(input instanceof Date)\n    ) {\n      value =\n        startOrEnd === 'start' ? input[barStart.value] : input[barEnd.value];\n    }\n    if (typeof input === 'string') {\n      value = input;\n    } else if (input instanceof Date) {\n      return dayjs(input);\n    }\n    const format = dateFormat.value || DEFAULT_DATE_FORMAT;\n    return dayjs(value, format, true);\n  };\n\n  const format = (input: string | Date | Dayjs, pattern?: string | false) => {\n    if (pattern === false) {\n      return input instanceof Date ? input : dayjs(input).toDate();\n    }\n    const inputDayjs =\n      typeof input === 'string' || input instanceof Date\n        ? toDayjs(input)\n        : input;\n\n    return inputDayjs.format(pattern);\n  };\n\n  return {\n    chartStartDayjs,\n    chartEndDayjs,\n    toDayjs,\n    format,\n  };\n}\n",
    "taille": 1644
  },
  {
    "microservice": "mesx-leveling-board-ui",
    "chemin": "/src/modules/leveling-board/components/Gantt/composables/useGridStyles.ts",
    "extension": ".ts",
    "prioritaire": false,
    "contenu": "import dayjs from 'dayjs';\nimport utc from 'dayjs/plugin/utc';\n// Import UTC plugin\ndayjs.extend(utc); // Extend dayjs with UTC support\n\nconst getGridStyles = (gridTime: string) => {\n  // Get the current time in ISO format\n  const currentTime = new Date().toISOString();\n\n  console.log('Received gridTime:', gridTime);\n  console.log('Current time (inside function):', currentTime);\n\n  // Parse both gridTime and currentTime as UTC\n  const gridTimeDayjs = dayjs.utc(gridTime);\n  const currentTimeDayjs = dayjs.utc(currentTime);\n\n  if (!gridTimeDayjs.isValid() || !currentTimeDayjs.isValid()) {\n    return { backgroundColor: 'transparent' }; // Default if dates are invalid\n  }\n\n  let backgroundColor = 'transparent';\n\n  // Check if gridTime is before the currentTime\n  if (gridTimeDayjs.isBefore(currentTimeDayjs)) {\n    backgroundColor = 'lightgrey'; // Grey for past times\n    console.log(\n      'Grid time is before current time. Setting backgroundColor to:',\n      backgroundColor,\n    );\n  } else if (gridTimeDayjs.isAfter(currentTimeDayjs)) {\n    backgroundColor = 'lightcoral'; // Red for future times\n    console.log(\n      'Grid time is after current time. Setting backgroundColor to:',\n      backgroundColor,\n    );\n  }\n\n  // Check if gridTime is within 1 hour before or after the currentTime\n  if (\n    gridTimeDayjs.isSameOrBefore(currentTimeDayjs) &&\n    gridTimeDayjs.add(1, 'hour').isSameOrAfter(currentTimeDayjs)\n  ) {\n    backgroundColor = 'lightblue'; // Color for the background within 1 hour before the current time\n    console.log(\n      'Grid time is within 1 hour before current time. Setting backgroundColor to:',\n      backgroundColor,\n    );\n  } else if (\n    gridTimeDayjs.isAfter(currentTimeDayjs) &&\n    gridTimeDayjs.subtract(1, 'hour').isSameOrBefore(currentTimeDayjs)\n  ) {\n    backgroundColor = 'lightblue'; // Color for the background within 1 hour after the current time\n    console.log(\n      'Grid time is within 1 hour after current time. Setting backgroundColor to:',\n      backgroundColor,\n    );\n  }\n\n  console.log('Final backgroundColor:', backgroundColor);\n\n  return { backgroundColor };\n};\n\nexport default getGridStyles;\n",
    "taille": 2164
  },
  {
    "microservice": "mesx-leveling-board-ui",
    "chemin": "/src/modules/leveling-board/components/Gantt/composables/useTimePositionMapping.ts",
    "extension": ".ts",
    "prioritaire": false,
    "contenu": "import { computed } from 'vue';\nimport type { GGanttChartConfig } from '../components/GGanttChart.vue';\n\nimport provideConfig from '../provider/provideConfig.js';\nimport useDayjsHelper from './useDayjsHelper.js';\n\n/**\n *\n * @param config\n */\nexport default function useTimePositionMapping(\n  config: GGanttChartConfig = provideConfig(),\n) {\n  const { dateFormat, chartSize } = config;\n  const { chartStartDayjs, chartEndDayjs, toDayjs, format } =\n    useDayjsHelper(config);\n\n  const totalNumberOfMinutes = computed(() => {\n    return chartEndDayjs.value.diff(chartStartDayjs.value, 'minutes');\n  });\n\n  const mapTimeToPosition = (time: string) => {\n    const width = chartSize.width.value;\n    console.log('width', width);\n    const diffFromStart = toDayjs(time).diff(\n      chartStartDayjs.value,\n      'minutes',\n      true,\n    );\n    return Math.ceil((diffFromStart / totalNumberOfMinutes.value) * width);\n  };\n\n  const mapPositionToTime = (xPos: number) => {\n    const width = chartSize.width.value || 0;\n    const diffFromStart = (xPos / width) * totalNumberOfMinutes.value;\n    return format(\n      chartStartDayjs.value.add(diffFromStart, 'minutes'),\n      dateFormat.value,\n    );\n  };\n\n  return {\n    mapTimeToPosition,\n    mapPositionToTime,\n  };\n}\n",
    "taille": 1261
  },
  {
    "microservice": "mesx-leveling-board-ui",
    "chemin": "/src/modules/leveling-board/components/Gantt/composables/useTimeaxisUnits.ts",
    "extension": ".ts",
    "prioritaire": false,
    "contenu": "import { computed } from 'vue';\nimport provideConfig from '../provider/provideConfig.js';\nimport useDayjsHelper from './useDayjsHelper.js';\n\n/**\n *\n */\nexport default function useTimeaxisUnits() {\n  const { precision } = provideConfig();\n  const { chartStartDayjs, chartEndDayjs } = useDayjsHelper();\n\n  const upperPrecision = computed(() => {\n    switch (precision?.value) {\n      case 'hour': {\n        return 'day';\n      }\n      case 'day': {\n        return 'month';\n      }\n      case 'date':\n      case 'week': {\n        return 'month';\n      }\n      case 'month': {\n        return 'year';\n      }\n      default: {\n        throw new Error(\n          \"Precision prop incorrect. Must be one of the following: 'hour', 'day', 'date', 'week', 'month'\",\n        );\n      }\n    }\n  });\n\n  const lowerPrecision = computed(() => {\n    switch (precision.value) {\n      case 'date': {\n        return 'day';\n      }\n      case 'week': {\n        return 'isoWeek';\n      }\n      default: {\n        return precision.value;\n      }\n    }\n  });\n\n  const displayFormats = {\n    hour: 'HH:mm',\n    date: 'DD.MMM',\n    day: 'DD.MMM',\n    week: 'WW',\n    month: 'MMMM YYYY',\n    year: 'YYYY',\n  };\n\n  const timeaxisUnits = computed(() => {\n    const upperUnits: Array<{\n      label: string;\n      value?: string;\n      date: Date;\n      width?: string;\n    }> = [];\n    const lowerUnits: Array<{\n      label: string;\n      value?: string;\n      date: Date;\n      width?: string;\n    }> = [];\n    const totalMinutes = chartEndDayjs.value.diff(\n      chartStartDayjs.value,\n      'minutes',\n      true,\n    );\n    const upperUnit = upperPrecision.value;\n    const lowerUnit = lowerPrecision.value;\n    let currentUpperUnit = chartStartDayjs.value;\n    let currentLowerUnit = chartStartDayjs.value;\n\n    while (currentLowerUnit.isSameOrBefore(chartEndDayjs.value)) {\n      const endCurrentLowerUnit = currentLowerUnit.endOf(lowerUnit);\n      const isLastItem = endCurrentLowerUnit.isAfter(chartEndDayjs.value);\n\n      const lowerWidth = isLastItem\n        ? (chartEndDayjs.value.diff(currentLowerUnit, 'minutes', true) /\n            totalMinutes) *\n          100\n        : (endCurrentLowerUnit.diff(currentLowerUnit, 'minutes', true) /\n            totalMinutes) *\n          100;\n\n      lowerUnits.push({\n        label: currentLowerUnit.format(displayFormats[precision.value]),\n        value: String(currentLowerUnit),\n        date: currentLowerUnit.toDate(),\n        width: `${String(lowerWidth)}%`,\n      });\n      currentLowerUnit = endCurrentLowerUnit\n        .add(1, lowerUnit === 'isoWeek' ? 'week' : lowerUnit)\n        .startOf(lowerUnit);\n    }\n\n    while (currentUpperUnit.isSameOrBefore(chartEndDayjs.value)) {\n      const endCurrentUpperUnit = currentUpperUnit.endOf(upperUnit);\n      const isLastItem = endCurrentUpperUnit.isAfter(chartEndDayjs.value);\n\n      const upperWidth = isLastItem\n        ? (chartEndDayjs.value.diff(currentUpperUnit, 'minutes', true) /\n            totalMinutes) *\n          100\n        : (endCurrentUpperUnit.diff(currentUpperUnit, 'minutes', true) /\n            totalMinutes) *\n          100;\n\n      upperUnits.push({\n        label: currentUpperUnit.format(displayFormats[upperUnit]),\n        value: String(currentUpperUnit),\n        date: currentUpperUnit.toDate(),\n        width: `${String(upperWidth)}%`,\n      });\n\n      currentUpperUnit = endCurrentUpperUnit\n        .add(1, upperUnit)\n        .startOf(upperUnit);\n    }\n\n    return { upperUnits, lowerUnits };\n  });\n\n  return {\n    timeaxisUnits,\n  };\n}\n",
    "taille": 3531
  },
  {
    "microservice": "mesx-leveling-board-ui",
    "chemin": "/src/modules/leveling-board/components/Gantt/playground.ts",
    "extension": ".ts",
    "prioritaire": false,
    "contenu": "import { createApp } from 'vue';\nimport Playground from './GanttPlayground.vue';\nimport ganttastic from './vue-ganttastic.js';\n\ncreateApp(Playground).use(ganttastic).mount('#app');\n",
    "taille": 181
  },
  {
    "microservice": "mesx-leveling-board-ui",
    "chemin": "/src/modules/leveling-board/components/Gantt/provider/provideEmitBarEvent.ts",
    "extension": ".ts",
    "prioritaire": false,
    "contenu": "import { inject } from 'vue';\nimport { EMIT_BAR_EVENT_KEY } from './symbols.js';\n\n/**\n *\n */\nexport default function provideEmitBarEvent() {\n  const emitBarEvent = inject(EMIT_BAR_EVENT_KEY);\n  if (!emitBarEvent) {\n    throw new Error('Failed to inject emitBarEvent!');\n  }\n  return emitBarEvent;\n}\n",
    "taille": 299
  },
  {
    "microservice": "mesx-leveling-board-ui",
    "chemin": "/src/modules/leveling-board/components/Gantt/provider/provideGetChartRows.ts",
    "extension": ".ts",
    "prioritaire": false,
    "contenu": "import { inject } from 'vue';\nimport { CHART_ROWS_KEY } from './symbols.js';\n\n/**\n *\n */\nexport default function provideGetChartRows() {\n  const getChartRows = inject(CHART_ROWS_KEY);\n  if (!getChartRows) {\n    throw new Error('Failed to inject getChartRows!');\n  }\n  return getChartRows;\n}\n",
    "taille": 291
  },
  {
    "microservice": "mesx-leveling-board-ui",
    "chemin": "/src/modules/leveling-board/components/Gantt/provider/symbols.ts",
    "extension": ".ts",
    "prioritaire": false,
    "contenu": "import type { InjectionKey, Ref } from 'vue';\n\nimport type { GGanttChartConfig } from '../components/GGanttChart.vue';\nimport type { GanttBarObject } from '../types';\n\nexport interface ChartRow {\n  fromLocation: string;\n  toLocation: string;\n  bars: Array<GanttBarObject>;\n}\nexport type GetChartRows = () => Array<ChartRow>;\nexport type EmitBarEvent = (\n  e: MouseEvent,\n  bar: GanttBarObject,\n  datetime?: string | Date,\n  movedBars?: Map<GanttBarObject, { oldStart: string; oldEnd: string }>,\n) => void;\n\nexport const CHART_ROWS_KEY = Symbol(\n  'CHART_ROWS_KEY',\n) as InjectionKey<GetChartRows>;\nexport const CONFIG_KEY = Symbol(\n  'CONFIG_KEY',\n) as InjectionKey<GGanttChartConfig>;\nexport const EMIT_BAR_EVENT_KEY = Symbol(\n  'EMIT_BAR_EVENT_KEY',\n) as InjectionKey<EmitBarEvent>;\nexport const BAR_CONTAINER_KEY = Symbol('BAR_CONTAINER_KEY') as InjectionKey<\n  Ref<HTMLElement | null>\n>;\n",
    "taille": 892
  },
  {
    "microservice": "mesx-leveling-board-ui",
    "chemin": "/src/modules/leveling-board/components/Gantt/types.ts",
    "extension": ".ts",
    "prioritaire": false,
    "contenu": "import type { CSSProperties } from 'vue';\n\nexport interface GanttBarObject {\n  // eslint-disable-next-line @typescript-eslint/no-explicit-any\n  [key: string]: any;\n  ganttBarConfig: {\n    id: string;\n    label?: string;\n    html?: string;\n    hasHandles?: boolean;\n    immobile?: boolean;\n    bundle?: string;\n    pushOnOverlap?: boolean;\n    dragLimitLeft?: number;\n    dragLimitRight?: number;\n    style?: CSSProperties;\n    class?: string;\n  };\n}\n",
    "taille": 450
  },
  {
    "microservice": "mesx-leveling-board-ui",
    "chemin": "/src/modules/leveling-board/components/Gantt/vue-ganttastic.ts",
    "extension": ".ts",
    "prioritaire": false,
    "contenu": "import dayjs from 'dayjs';\nimport advancedFormat from 'dayjs/plugin/advancedFormat';\nimport customParseFormat from 'dayjs/plugin/customParseFormat.js';\nimport isBetween from 'dayjs/plugin/isBetween.js';\nimport isSameOrAfter from 'dayjs/plugin/isSameOrAfter.js';\nimport isSameOrBefore from 'dayjs/plugin/isSameOrBefore.js';\nimport isoWeek from 'dayjs/plugin/isoWeek';\nimport weekOfYear from 'dayjs/plugin/weekOfYear';\nimport type { Plugin } from 'vue';\n\nimport GGanttChart from './components/GGanttChart.vue';\nimport GGanttRow from './components/GGanttRow.vue';\n\n/**\n *\n */\nexport function extendDayjs() {\n  dayjs.extend(isSameOrBefore);\n  dayjs.extend(isSameOrAfter);\n  dayjs.extend(isBetween);\n  dayjs.extend(customParseFormat);\n  dayjs.extend(weekOfYear);\n  dayjs.extend(isoWeek);\n  dayjs.extend(advancedFormat);\n}\n\nexport const ganttastic: Plugin = {\n  install(app) {\n    extendDayjs();\n    app.component('GGanttChart', GGanttChart);\n    app.component('GGanttRow', GGanttRow);\n  },\n};\n\nexport default ganttastic;\n\nexport { default as GGanttChart } from './components/GGanttChart.vue';\nexport { type ColorScheme } from './color-schemes';\nexport { type GanttBarObject } from './types.js';\n\nexport { default as GGanttRow } from './components/GGanttRow.vue';\n",
    "taille": 1258
  },
  {
    "microservice": "mesx-leveling-board-ui",
    "chemin": "/src/modules/leveling-board/dto/auth.config.dto.ts",
    "extension": ".ts",
    "prioritaire": false,
    "contenu": "import { WebStorageStateStore } from 'oidc-client';\n\nexport class AuthConfigDTO {\n  public authority: string;\n\n  public client_id: string;\n\n  public redirect_uri: string;\n\n  public response_type: string;\n\n  public scope: string;\n\n  public userStore: WebStorageStateStore;\n\n  public accessTokenExpiringNotificationTime?: number;\n\n  public constructor(\n    authority: string,\n    client_id: string,\n    redirect_uri: string,\n    response_type: string,\n    scope: string,\n    userStore: WebStorageStateStore,\n    accessTokenExpiringNotificationTime: number,\n  ) {\n    this.authority = authority;\n    this.client_id = client_id;\n    this.redirect_uri = redirect_uri;\n    this.response_type = response_type;\n    this.scope = scope;\n    this.userStore = userStore;\n    this.accessTokenExpiringNotificationTime =\n      accessTokenExpiringNotificationTime;\n  }\n}\n",
    "taille": 855
  },
  {
    "microservice": "mesx-leveling-board-ui",
    "chemin": "/src/modules/leveling-board/dto/plant-response.dto.ts",
    "extension": ".ts",
    "prioritaire": false,
    "contenu": "import { WkOrder } from './wk-order.dto';\n\nexport class PlantResponse {\n  public plantId: string;\n\n  public aggrTimeLvlBoard: number;\n\n  public wkOrderList: Array<WkOrder>;\n\n  public toleranceValueWk: number;\n\n  public constructor(\n    plantId: string,\n    aggrTimeLvlBoard: number,\n    toleranceValueWk: number,\n    wkOrderList: Array<WkOrder>,\n  ) {\n    this.plantId = plantId;\n    this.aggrTimeLvlBoard = aggrTimeLvlBoard;\n    this.wkOrderList = wkOrderList;\n    this.toleranceValueWk = toleranceValueWk;\n  }\n}\n",
    "taille": 514
  },
  {
    "microservice": "mesx-leveling-board-ui",
    "chemin": "/src/modules/leveling-board/dto/query-parameters.ts",
    "extension": ".ts",
    "prioritaire": false,
    "contenu": "export interface QueryParameters {\n  closedWKs?: boolean;\n  customer?: string;\n  workCenter?: string;\n  uap?: string;\n  pickingOrder?: string;\n  outboundDelivery?: string;\n  material?: string;\n  destinationTypeList?: string;\n  customStatusList?: string;\n  sourceTypeList?: string;\n  tpaNumberList?: string;\n  filterGroupingOne?: string;\n  filterGroupingTwo?: string;\n  startPreparationLabel?: string;\n  customStartPreparationDate?: string;\n  truckDepartureLabel?: string;\n  customTruckDepartureDate?: string;\n  pickingStatusList?: Array<string>;\n}\n",
    "taille": 548
  },
  {
    "microservice": "mesx-leveling-board-ui",
    "chemin": "/src/modules/leveling-board/dto/user.ts",
    "extension": ".ts",
    "prioritaire": false,
    "contenu": "export interface User {\n  userId: string;\n}\n",
    "taille": 44
  },
  {
    "microservice": "mesx-leveling-board-ui",
    "chemin": "/src/modules/leveling-board/dto/userPreference.ts",
    "extension": ".ts",
    "prioritaire": false,
    "contenu": "import { User } from './user.ts';\n\nexport interface UserPreference {\n  plant: number;\n  action: string;\n  appName: string;\n  userCriteria: string;\n  user: User;\n}\n",
    "taille": 163
  },
  {
    "microservice": "mesx-leveling-board-ui",
    "chemin": "/src/modules/leveling-board/dto/wk-order.dto.ts",
    "extension": ".ts",
    "prioritaire": false,
    "contenu": "export class WkOrder {\n  public wkOrder: string;\n\n  public truckDepartureTime: string;\n\n  public tpaEndPreparation: string;\n\n  public tpaBeginPreparation: string;\n\n  public advancePickingDuration: string;\n\n  public plannedStartDate: string;\n\n  public customer: string;\n\n  public customerDesc: string;\n\n  public plannedItem: string;\n\n  public sebango: string;\n\n  public wcDesc: string;\n\n  public wcCode: string;\n\n  public erpDelNo: string;\n\n  public customStatus: string;\n\n  public status: string;\n\n  public color: string;\n\n  public closeable: string;\n\n  public sourceLocation: string;\n\n  public destinationLocation: string;\n\n  public truck: string;\n\n  public tpaNumber: string;\n\n  public shopOrder: string;\n\n  public material: string;\n\n  public pickingTime: string;\n\n  public pickingOrderId: string;\n\n  public sourceType: string;\n\n  public destinationType: string;\n\n  public newPromiseTime: string;\n\n  public alertCreated: boolean;\n\n  public alertCreationTime: string;\n\n  public launcherWaitingTime?: string;\n\n  public maxLauncherWaitingTime?: string | number;\n\n  public minLauncherWaitingTime?: string | number;\n\n  // Refactored constructor accepting an object as a parameter\n  public constructor(data: {\n    wkOrder: string;\n    truckDepartureTime: string;\n    tpaEndPreparation: string;\n    tpaBeginPreparation: string;\n    advancePickingDuration: string;\n    plannedStartDate: string;\n    customer: string;\n    customerDesc: string;\n    plannedItem: string;\n    sebango: string;\n    wcDesc: string;\n    wcCode: string;\n    erpDelNo: string;\n    customStatus: string;\n    status: string;\n    color: string;\n    closeable: string;\n    sourceLocation: string;\n    destinationLocation: string;\n    truck: string;\n    tpaNumber: string;\n    shopOrder: string;\n    material: string;\n    pickingTime: string;\n    pickingOrderId: string;\n    sourceType: string;\n    destinationType: string;\n    newPromiseTime: string;\n    alertCreated: boolean;\n    alertStatus: string;\n    alertCreationTime: string;\n    launcherWaitingTime?: {\n      value: number | string;\n    };\n  }) {\n    // Destructure the data object\n    this.wkOrder = data.wkOrder;\n    this.truckDepartureTime = data.truckDepartureTime;\n    this.tpaEndPreparation = data.tpaEndPreparation;\n    this.tpaBeginPreparation = data.tpaBeginPreparation;\n    this.advancePickingDuration = data.advancePickingDuration;\n    this.plannedStartDate = data.plannedStartDate;\n    this.customer = data.customer;\n    this.customerDesc = data.customerDesc;\n    this.plannedItem = data.plannedItem;\n    this.sebango = data.sebango;\n    this.wcDesc = data.wcDesc;\n    this.wcCode = data.wcCode;\n    this.erpDelNo = data.erpDelNo;\n    this.customStatus = data.customStatus;\n    this.status = data.status;\n    this.color = data.color;\n    this.closeable = data.closeable;\n    this.sourceLocation = data.sourceLocation;\n    this.destinationLocation = data.destinationLocation;\n    this.truck = data.truck;\n    this.tpaNumber = data.tpaNumber;\n    this.shopOrder = data.shopOrder;\n    this.material = data.material;\n    this.pickingTime = data.pickingTime;\n    this.pickingOrderId = data.pickingOrderId;\n    this.sourceType = data.sourceType;\n    this.destinationType = data.destinationType;\n    this.newPromiseTime = data.newPromiseTime;\n    this.alertCreated = data.alertCreated;\n    this.alertCreationTime = data.alertCreationTime;\n    this.launcherWaitingTime = data.launcherWaitingTime?.value?.toString();\n  }\n}\n",
    "taille": 3449
  },
  {
    "microservice": "mesx-leveling-board-ui",
    "chemin": "/src/modules/leveling-board/dto/workcenter-details.dto.ts",
    "extension": ".ts",
    "prioritaire": false,
    "contenu": "export interface WorkcenterDetails {\n  advancePickingDuration?: string;\n  alertCreation?: string;\n  description?: string;\n  maxLauncherWaitingTime?: string;\n  minLauncherWaitingTime?: string;\n  uapId?: string;\n}\n",
    "taille": 212
  },
  {
    "microservice": "mesx-leveling-board-ui",
    "chemin": "/src/modules/leveling-board/filter-config/dtos/filter-config.dto.ts",
    "extension": ".ts",
    "prioritaire": false,
    "contenu": "export interface FilterConfig {\n  id?: number;\n  appName: string;\n  userCriteria: string;\n  groupName: string;\n  plant: string;\n  createdAt?: string;\n}\n",
    "taille": 152
  },
  {
    "microservice": "mesx-leveling-board-ui",
    "chemin": "/src/modules/leveling-board/filter-config/services/api.ts",
    "extension": ".ts",
    "prioritaire": false,
    "contenu": "import axios, { AxiosResponse } from 'axios';\nimport { FilterConfig } from '../dtos/filter-config.dto';\n\nconst umeBaseUrl = import.meta.env.UME_API_URL;\nlet token = import.meta.env.TOKEN;\ntoken =\n  token === 'PING_FEDERATE'\n    ? (sessionStorage.getItem('id_token') ?? '')\n    : token;\n\nconst axiosInstance = axios.create({\n  baseURL: umeBaseUrl,\n  headers: {\n    authorization: `Bearer ${token}`,\n  },\n});\n\nexport const getGroupNamesByUserAndPlant = async (\n  userId: number,\n  plant: string,\n  appName: string,\n): Promise<Array<string>> => {\n  const response: AxiosResponse<Array<string>> = await axiosInstance.get(\n    '/filter-config/group-names-by-user',\n    {\n      params: { userId, plant, appName },\n    },\n  );\n  return response.data;\n};\n\nexport const getFilterConfigByPlant = async (\n  plant: string,\n  appName: string,\n): Promise<Array<FilterConfig>> => {\n  const response: AxiosResponse<Array<FilterConfig>> = await axiosInstance.get(\n    '/filter-config/find-by-plant',\n    {\n      params: { plant, appName },\n    },\n  );\n  return response.data;\n};\n\nexport const createFilterConfig = async (\n  data: FilterConfig,\n): Promise<FilterConfig> => {\n  const response: AxiosResponse<FilterConfig> = await axiosInstance.post(\n    '/filter-config',\n    data,\n  );\n  return response.data;\n};\n\nexport const updateFilterConfig = async (\n  id: number,\n  data: FilterConfig,\n): Promise<FilterConfig> => {\n  const response: AxiosResponse<FilterConfig> = await axiosInstance.put(\n    `/filter-config/${id}`,\n    data,\n  );\n  return response.data;\n};\n\nexport const deleteFilterConfig = async (id: number): Promise<void> => {\n  await axiosInstance.delete(`/filter-config/${id}`);\n};\n\nexport const getFilterByUserAndGroup = async (\n  plant: string,\n  groupName: string,\n  appName: string,\n): Promise<FilterConfig> => {\n  const response: AxiosResponse<FilterConfig> = await axiosInstance.get(\n    '/filter-config/find-by-user-group',\n    {\n      params: { plant, groupName, appName },\n    },\n  );\n  return response.data;\n};\n",
    "taille": 2017
  },
  {
    "microservice": "mesx-leveling-board-ui",
    "chemin": "/src/modules/leveling-board/service/api.ts",
    "extension": ".ts",
    "prioritaire": false,
    "contenu": "import axios from 'axios';\nimport { UserPreference } from '../dto/userPreference';\n\nconst umeBaseUrl = import.meta.env.UME_API_URL;\n\nlet token = import.meta.env.TOKEN;\ntoken = token === 'PING_FEDERATE' ? sessionStorage.getItem('id_token') : token;\n\n/**\n * @param preferences - Array of user preferences to save.\n * @returns - A promise resolving the response data.\n */\nexport const saveUserPreferences = async (\n  preferences: Array<UserPreference>,\n): Promise<string> => {\n  /* eslint no-useless-catch: \"off\" */\n  const response = await axios.post(\n    `${umeBaseUrl}/user-preference/save`,\n    preferences,\n    {\n      headers: {\n        Authorization: `Bearer ${token}`,\n      },\n    },\n  );\n\n  return response.data;\n};\n/**\n * Fetches user preferences by plant, user ID, and app name.\n * @param plant - The plant ID to filter preferences.\n * @param userId - The user ID to filter preferences.\n * @param appName - The application name to filter preferences.\n * @returns A promise that resolves to an array of UserPreferenceResponseDto.\n */\nexport const fetchUserPreferenceByPlantUserIdAndAppName = async (\n  plant: number,\n  userId: number,\n  appName: string,\n): Promise<Array<UserPreference>> => {\n  try {\n    // Construct the URL with path parameters\n    const url = `${umeBaseUrl}/user-preference/get/${plant}/${userId}/${appName}`;\n\n    // Make the API request\n    const response = await axios.get(url, {\n      headers: {\n        Authorization: `Bearer ${token}`,\n      },\n    });\n\n    // Return the response data as an array of UserPreference\n    return response.data as Array<UserPreference>;\n  } catch (error) {\n    console.error('Error fetching user preferences:', error);\n    throw error;\n  }\n};\n",
    "taille": 1707
  },
  {
    "microservice": "mesx-leveling-board-ui",
    "chemin": "/src/modules/leveling-board/service/redirect.ts",
    "extension": ".ts",
    "prioritaire": false,
    "contenu": "/**\n * Cleans up expired or corrupted redirect sessions from localStorage.\n *\n * A redirect session is considered expired if its `expiresAt` timestamp\n * is less than the current time.\n *\n * This function should be called periodically (e.g. on app startup)\n * to prevent accumulation of stale sessions.\n * @example\n * cleanupRedirectSessions();\n * // Removes all expired redirectSession_* entries from localStorage\n */\nfunction cleanupRedirectSessions(): void {\n  const now = Date.now();\n\n  for (const key of Object.keys(localStorage)) {\n    if (key.startsWith('redirectSession_')) {\n      try {\n        const payload = JSON.parse(localStorage.getItem(key) as string);\n        if (!payload || payload.expiresAt < now) {\n          localStorage.removeItem(key);\n        }\n      } catch {\n        localStorage.removeItem(key); // Corrupted â delete\n      }\n    }\n  }\n}\n\n/**\n * Creates a new redirect session in localStorage.\n *\n * The session key will be prefixed with `redirectSession_` followed by a unique UUID.\n * The value stored is a JSON string containing:\n * - `data`: arbitrary details you want to share between apps\n * - `expiresAt`: expiration timestamp in milliseconds since epoch\n * @param data - The data object to persist in the session (must be JSON-serializable).\n * @returns The generated session key (e.g. \"redirectSession_123abc\").\n * @example\n * const key = createRedirectSession({ filters: { status: 'open' } });\n * // -> \"redirectSession_abcd-1234...\"\n * // Then pass this key (or just the UUID part) to App2 via URL\n */\nfunction createRedirectSession(data: unknown): string {\n  const sessionId = crypto.randomUUID();\n  const createdAt = Date.now();\n  const expiresAt = createdAt + 5 * 60 * 1000; // 5 minutes from now\n  const redirectSession = `redirectSession_${sessionId}`;\n  cleanupRedirectSessions();\n\n  const payload = {\n    createdAt,\n    data,\n    expiresAt,\n  };\n\n  localStorage.setItem(redirectSession, JSON.stringify(payload));\n  return redirectSession;\n}\n\n/**\n * Recursively extracts all values of a given property name\n * from an object of arbitrary structure.\n * @param object - The object to search through.\n * @param propertyName - The property name to extract values for.\n * @returns An array of values found for the property.\n * @example\n * const json = '{\"foo\":{\"bar\":[{\"shopOrder\":\"ORDER_1\"},{\"shopOrder\":\"ORDER_2\"}]}}';\n * const data = JSON.parse(json);\n * const orders = extractValues(data, \"shopOrder\");\n * console.log(orders); // [\"ORDER_1\", \"ORDER_2\"]\n */\nfunction extractValues(object: unknown, propertyName: string): Array<string> {\n  const results: Array<string> = [];\n  /**\n   * Helper function to recursively traverse arrays/objects.\n   * @param value - Current value being traversed.\n   */\n  function recurse(value: unknown): void {\n    if (Array.isArray(value)) {\n      for (const item of value) {\n        recurse(item);\n      }\n    } else if (value && typeof value === 'object') {\n      for (const [key, innerValue] of Object.entries(value)) {\n        if (key === propertyName) {\n          results.push(innerValue as string);\n        }\n        recurse(innerValue);\n      }\n    }\n  }\n\n  recurse(object);\n  return results;\n}\n\nexport { createRedirectSession, extractValues };\n",
    "taille": 3228
  },
  {
    "microservice": "mesx-leveling-board-ui",
    "chemin": "/src/quasar-user-options.ts",
    "extension": ".ts",
    "prioritaire": false,
    "contenu": "import './styles/quasar.sass';\nimport '@quasar/extras/roboto-font/roboto-font.css';\nimport '@quasar/extras/material-icons/material-icons.css';\nimport '@quasar/extras/material-icons-outlined/material-icons-outlined.css';\nimport '@quasar/extras/material-icons-round/material-icons-round.css';\nimport '@quasar/extras/material-icons-sharp/material-icons-sharp.css';\nimport '@quasar/extras/fontawesome-v5/fontawesome-v5.css';\nimport { Notify, Loading } from 'quasar';\n// To be used on app.use(Quasar, { ... })\nexport default {\n  config: {},\n  plugins: { Notify, Loading },\n  extras: ['fontawesome-v5'],\n  framework: {\n    iconSet: 'fontawesome-v5',\n  },\n};\n",
    "taille": 652
  },
  {
    "microservice": "mesx-leveling-board-ui",
    "chemin": "/src/router/index.ts",
    "extension": ".ts",
    "prioritaire": false,
    "contenu": "import { Logout } from '@mesx/common-ui-lib';\nimport { createRouter, createWebHistory } from 'vue-router';\nimport advancedFilter from '../modules/leveling-board/filter-config/views/advanced-filter.vue';\nimport Unauthorized from '../modules/leveling-board/views/Unauthorized.vue';\nimport LevelingTimlineBoard from '../modules/leveling-board/views/leveling-board-v2.vue';\nimport LevelTimelineScndVersion from '../modules/leveling-board/views/leveling-board-v3.vue';\nimport loadingComponent from '../modules/leveling-board/views/loading.vue';\n// Import LevelingBoard from '../modules/leveling-board/views/leveling-board.vue';\nimport { useProfileStore } from '../store/user-auth-store.ts';\nimport appLogger from '../utils/logger/app-logger.ts';\n\nconst appRedirectUri = import.meta.env.APP_REDIRECT_URI;\nlet appBasePath: string | undefined;\nif (appRedirectUri) {\n  const parts = appRedirectUri.split('/');\n  appBasePath = parts.pop();\n}\nconst routes = [\n  {\n    component: LevelingTimlineBoard,\n    meta: { requiresAuth: true, title: 'Leveling timeline Board' },\n    name: 'LevelingTimlineBoard',\n    path: '/lvl-ui',\n    props: true,\n  },\n  {\n    component: LevelingTimlineBoard,\n    meta: { requiresAuth: true, title: 'Leveling timeline Board' },\n    name: 'SharedGroupFilterBoard',\n    path: '/lvl-ui/:groupName',\n    props: true,\n  },\n  {\n    component: advancedFilter,\n    meta: { requiresAuth: true, title: 'Filter Layout' },\n    name: 'Filter Layout',\n    path: '/lvl-ui/filter-layout',\n    props: true,\n  },\n  {\n    component: LevelTimelineScndVersion,\n    meta: { requiresAuth: true, title: 'Leveling vis timeline Board' },\n    name: 'LevelTimelineScndVersion',\n    path: '/lvl-ui-timeline-v2/',\n    props: true,\n  },\n  {\n    component: Logout,\n    name: 'Logout',\n    path: '/logout',\n    props: {\n      onLoginAgain: (): void => {\n        const profileStore = useProfileStore();\n        profileStore.initializeAuthentication();\n        router.push('/lvl-ui');\n      },\n    },\n  },\n  {\n    component: Unauthorized,\n    name: 'Unauthorized',\n    path: `/${appBasePath}/unauthorized`,\n  },\n  {\n    component: loadingComponent,\n    name: 'LoadingComponent',\n    path: `/${appBasePath}/loading`,\n  },\n];\nexport const router = createRouter({\n  history: createWebHistory(),\n  routes,\n});\n\nrouter.beforeEach(async (to, _from, next) => {\n  if (to.meta.title) {\n    document.title = `MESX.0 - ${to.meta.title as string}`;\n  }\n  const userAuth = useProfileStore();\n  if (to.meta.requiresAuth) {\n    try {\n      await userAuth.authenticateUser();\n      const user = await userAuth.getUser();\n      if (user) {\n        await userAuth.fetchUserAttributes();\n        await userAuth.authorizeUser();\n      } else {\n        // Handle the case where the user is null\n        console.error('User not found');\n        return next(`/${appBasePath}/unauthorized`); // Return is added here\n      }\n\n      // Proceed to the route\n      return next(); // Explicit return added\n    } catch (error) {\n      appLogger.error(`Error during authentication or authorization: ${error}`);\n      return next(`/${appBasePath}/unauthorized`); // Return is added here\n    }\n  } else {\n    return next(); // Explicit return for non-authenticated routes\n  }\n});\n",
    "taille": 3228
  },
  {
    "microservice": "mesx-leveling-board-ui",
    "chemin": "/src/services/api-service.ts",
    "extension": ".ts",
    "prioritaire": false,
    "contenu": "import axios from 'axios';\nimport { PlantResponse } from 'src/modules/leveling-board/dto/plant-response.dto';\nimport { QueryParameters } from '../modules/leveling-board/dto/query-parameters';\nimport { WorkcenterDetails } from '../modules/leveling-board/dto/workcenter-details.dto';\n\nconst API_BASE_URL = import.meta.env.LEVELING_BACKEND_URL;\nconst token =\n  import.meta.env.TOKEN === undefined ? undefined : import.meta.env.TOKEN;\n\nexport const apiService = {\n  async getWorkcenterDetailsById(\n    plantId: number,\n    workcenterId: string,\n  ): Promise<WorkcenterDetails> {\n    try {\n      const apiUrl = `${API_BASE_URL}/wk/getWorkcenterDetails/${plantId}/${workcenterId}`;\n      const headers = token ? { authorization: `Bearer ${token}` } : {};\n      const response = await axios.get(apiUrl, {\n        withCredentials: true,\n        headers,\n      });\n      return response.data;\n    } catch (error: unknown) {\n      console.error('Error fetching workcenter details:', error);\n      if (typeof error === 'object' && error !== null && 'response' in error) {\n        throw new Error(\n          `HTTP error! Status: ${(error as { response: { status: number } }).response.status},`,\n        );\n      } else if (\n        typeof error === 'object' &&\n        error !== null &&\n        'request' in error\n      ) {\n        throw new Error('No response received from the server.');\n      } else {\n        throw new Error(`Error in request setup: ${(error as Error).message}`);\n      }\n    }\n  },\n  async getWorkOrder(\n    plant: number,\n    startDate: string,\n    endDate: string,\n    queryParameters?: {\n      closedWKs?: boolean;\n      customer?: string;\n      workCenter?: string;\n      uap?: string;\n      pickingOrder?: string;\n      outboundDelivery?: string;\n      material?: string;\n      destinationTypeList?: string;\n      customStatusList?: string;\n      sourceTypeList?: string;\n      tpaNumberList?: string;\n      filterGroupingOne?: string;\n      filterGroupingTwo?: string;\n      startPreparationLabel?: string;\n      customStartPreparationDate?: string;\n      truckDepartureLabel?: string;\n      customTruckDepartureDate?: string;\n      pickingStatusList?: Array<string>;\n    },\n  ): Promise<PlantResponse> {\n    try {\n      let apiUrl = `${API_BASE_URL}/wk/getWkOrderByCriteria/${plant}`;\n      const parameter = queryParameters ?? {};\n\n      const queryString = this.buildQueryString(parameter);\n\n      if (queryString) {\n        apiUrl += `?${queryString}`;\n      }\n\n      const parameters: object = {\n        startDate,\n        endDate,\n      };\n\n      const response = await axios.get(apiUrl, {\n        params: parameters,\n        withCredentials: true,\n        headers: token ? { authorization: `Bearer ${token}` } : {},\n      });\n\n      return response.data;\n    } catch (error: any) {\n      console.error('Error fetching work order:', error);\n\n      if (error.response) {\n        throw new Error(`HTTP error! Status: ${error.response.status}`);\n      } else if (error.request) {\n        throw new Error('No response received from the server.');\n      } else {\n        throw new Error(`Error in request setup: ${error.message}`);\n      }\n    }\n  },\n\n  buildQueryString(parameters: QueryParameters): string {\n    const queryStrings = [];\n    const {\n      closedWKs,\n      customer,\n      workCenter,\n      uap,\n      pickingOrder,\n      outboundDelivery,\n      material,\n      destinationTypeList,\n      customStatusList,\n      sourceTypeList,\n      tpaNumberList,\n      filterGroupingOne,\n      filterGroupingTwo,\n      startPreparationLabel,\n      customStartPreparationDate,\n      truckDepartureLabel,\n      customTruckDepartureDate,\n      pickingStatusList,\n    } = parameters;\n\n    if (closedWKs)\n      queryStrings.push(`closedWKs=${encodeURIComponent(closedWKs)}`);\n    if (customer)\n      queryStrings.push(`customerList=${encodeURIComponent(customer)}`);\n    if (workCenter)\n      queryStrings.push(`workCenterList=${encodeURIComponent(workCenter)}`);\n    if (uap) queryStrings.push(`uapList=${encodeURIComponent(uap)}`);\n    if (pickingOrder)\n      queryStrings.push(`pickingOrder=${encodeURIComponent(pickingOrder)}`);\n    if (outboundDelivery)\n      queryStrings.push(`outboundList=${encodeURIComponent(outboundDelivery)}`);\n    if (material)\n      queryStrings.push(`materialList=${encodeURIComponent(material)}`);\n    if (sourceTypeList)\n      queryStrings.push(`sourceTypeList=${encodeURIComponent(sourceTypeList)}`);\n    if (destinationTypeList)\n      queryStrings.push(\n        `destinationTypeList=${encodeURIComponent(destinationTypeList)}`,\n      );\n    if (pickingStatusList?.length) {\n      for (const status of pickingStatusList) {\n        queryStrings.push(`pickingStatusList[]=${encodeURIComponent(status)}`);\n      }\n    }\n    if (customStatusList)\n      queryStrings.push(\n        `customStatusList=${encodeURIComponent(customStatusList)}`,\n      );\n    if (tpaNumberList)\n      queryStrings.push(`tpaNumberList=${encodeURIComponent(tpaNumberList)}`);\n    if (filterGroupingOne)\n      queryStrings.push(\n        `filterGroupingOne=${encodeURIComponent(filterGroupingOne)}`,\n      );\n    if (filterGroupingTwo)\n      queryStrings.push(\n        `filterGroupingTwo=${encodeURIComponent(filterGroupingTwo)}`,\n      );\n    if (startPreparationLabel !== undefined)\n      queryStrings.push(\n        `startPreparation=${encodeURIComponent(startPreparationLabel)}`,\n      );\n    if (customStartPreparationDate !== undefined)\n      queryStrings.push(\n        `startPreparationDate=${encodeURIComponent(customStartPreparationDate)}`,\n      );\n    if (truckDepartureLabel !== undefined)\n      queryStrings.push(\n        `truckDeparture=${encodeURIComponent(truckDepartureLabel)}`,\n      );\n    if (customTruckDepartureDate !== undefined)\n      queryStrings.push(\n        `truckDepartureDate=${encodeURIComponent(customTruckDepartureDate)}`,\n      );\n\n    return queryStrings.join('&');\n  },\n\n  async getMaterialList(criteriaId: number): Promise<Array<string>> {\n    try {\n      const apiUrl = `${API_BASE_URL}/filters/getMaterialList/${criteriaId}`;\n\n      const response = await axios.post(apiUrl, {\n        withCredentials: true,\n        headers: token ? { authorization: `Bearer ${token}` } : {},\n      });\n      return response.data;\n    } catch (error: any) {\n      console.error('Error fetching MaterialList:', error);\n\n      if (error.response) {\n        throw new Error(`HTTP error! Status: ${error.response.status}`);\n      } else if (error.request) {\n        throw new Error('No response received from the server.');\n      } else {\n        throw new Error(`Error in request setup: ${error.message}`);\n      }\n    }\n  },\n\n  async getCustomerList(\n    criteriaId: number,\n    queryParameters: {\n      material?: Array<string>;\n      workCenter?: Array<string>;\n      pickingOrder?: Array<string>;\n      outboundDelivery?: Array<string>;\n    },\n  ): Promise<any> {\n    try {\n      const apiUrl = `${API_BASE_URL}/filters/getCustomerList/${criteriaId}`;\n\n      const postData = {\n        material: queryParameters.material ?? undefined,\n        outboundDelivery: queryParameters.outboundDelivery ?? undefined,\n        pickingOrder: queryParameters.pickingOrder ?? undefined,\n        workCenter: queryParameters.workCenter ?? undefined,\n      };\n\n      const response = await axios.post(apiUrl, postData, {\n        withCredentials: true,\n        headers: token ? { authorization: `Bearer ${token}` } : {},\n      });\n      return response.data;\n    } catch (error: any) {\n      console.error('Error fetching CustomerList:', error);\n\n      if (error.response) {\n        throw new Error(`HTTP error! Status: ${error.response.status}`);\n      } else if (error.request) {\n        throw new Error('No response received from the server.');\n      } else {\n        throw new Error(`Error in request setup: ${error.message}`);\n      }\n    }\n  },\n\n  async getOutboundDeliveryList(\n    criteriaId: number,\n    queryParameters: {\n      material?: Array<string>;\n      pickingOrder?: Array<string>;\n      customer?: Array<string>;\n    },\n  ): Promise<any> {\n    try {\n      const apiUrl = `${API_BASE_URL}/filters/getOdList/${criteriaId}`;\n\n      const postData = {\n        customer: queryParameters.customer ?? undefined,\n        material: queryParameters.material ?? undefined,\n        pickingOrder: queryParameters.pickingOrder ?? undefined,\n      };\n\n      const response = await axios.post(apiUrl, postData, {\n        withCredentials: true,\n        headers: token ? { authorization: `Bearer ${token}` } : {},\n      });\n      return response.data;\n    } catch (error: any) {\n      console.error('Error fetching OutboundDeliveryList:', error);\n\n      if (error.response) {\n        throw new Error(`HTTP error! Status: ${error.response.status}`);\n      } else if (error.request) {\n        throw new Error('No response received from the server.');\n      } else {\n        throw new Error(`Error in request setup: ${error.message}`);\n      }\n    }\n  },\n\n  async getTpaList(\n    criteriaId: number,\n    queryParameters: {\n      customer?: Array<string>;\n      outboundDelivery?: Array<string>;\n    },\n  ): Promise<any> {\n    try {\n      const apiUrl = `${API_BASE_URL}/filters/getTpaList/${criteriaId}`;\n\n      const postData = {\n        customer: queryParameters.customer ?? undefined,\n        outboundDelivery: queryParameters.outboundDelivery ?? undefined,\n      };\n\n      const response = await axios.post(apiUrl, postData, {\n        withCredentials: true,\n        headers: token ? { authorization: `Bearer ${token}` } : {},\n      });\n      return response.data;\n    } catch (error: any) {\n      console.error('Error fetching TpaList:', error);\n\n      if (error.response) {\n        throw new Error(`HTTP error! Status: ${error.response.status}`);\n      } else if (error.request) {\n        throw new Error('No response received from the server.');\n      } else {\n        throw new Error(`Error in request setup: ${error.message}`);\n      }\n    }\n  },\n\n  async getShopOrderList(\n    criteriaId: number,\n    queryParameters: {\n      material?: Array<string>;\n      workCenter?: Array<string>;\n      customer?: Array<string>;\n      outboundDelivery?: Array<string>;\n    },\n  ): Promise<any> {\n    try {\n      const apiUrl = `${API_BASE_URL}/filters/getPickingOrderList/${criteriaId}`;\n\n      const postData = {\n        customer: queryParameters.customer ?? undefined,\n        material: queryParameters.material ?? undefined,\n        outboundDelivery: queryParameters.outboundDelivery ?? undefined,\n        workCenter: queryParameters.workCenter ?? undefined,\n      };\n\n      const response = await axios.post(apiUrl, postData, {\n        withCredentials: true,\n        headers: token ? { authorization: `Bearer ${token}` } : {},\n      });\n      return response.data;\n    } catch (error: any) {\n      console.error('Error fetching ShopOrderList:', error);\n\n      if (error.response) {\n        throw new Error(`HTTP error! Status: ${error.response.status}`);\n      } else if (error.request) {\n        throw new Error('No response received from the server.');\n      } else {\n        throw new Error(`Error in request setup: ${error.message}`);\n      }\n    }\n  },\n\n  async getWorkCentersList(\n    plantId: number, // `plantId` corresponds to the `plant` path parameter\n  ): Promise<any> {\n    try {\n      const apiUrl = `${API_BASE_URL}/filters/getWorkCentersGroupedByUaps/${plantId}`;\n      const response = await axios.post(apiUrl, {\n        withCredentials: true,\n        headers: token ? { authorization: `Bearer ${token}` } : {},\n      });\n\n      return response.data;\n    } catch (error: any) {\n      console.error('Error fetching WorkCentersList:', error);\n\n      if (error.response) {\n        throw new Error(`HTTP error! Status: ${error.response.status}`);\n      } else if (error.request) {\n        throw new Error('No response received from the server.');\n      } else {\n        throw new Error(`Error in request setup: ${error.message}`);\n      }\n    }\n  },\n\n  async updateWorkCenterAlertMode(\n    plantId: number,\n    workCenterCode: string,\n    alertMode: string,\n  ): Promise<{ success: boolean }> {\n    try {\n      const apiUrl = `${API_BASE_URL}/wk/updateAlertMode`;\n      const response = await axios.post(\n        apiUrl,\n        { plantId, workCenterCode, alertMode },\n        {\n          withCredentials: true,\n          headers: token ? { authorization: `Bearer ${token}` } : {},\n        },\n      );\n      return response.data;\n    } catch (error: unknown) {\n      console.error('Error updating alert mode:', error);\n      if (typeof error === 'object' && error !== null && 'response' in error) {\n        throw new Error(\n          `HTTP error! Status: ${(error as { response: { status: number } }).response.status},`,\n        );\n      } else if (\n        typeof error === 'object' &&\n        error !== null &&\n        'request' in error\n      ) {\n        throw new Error('No response received from the server.');\n      } else {\n        throw new Error(`Error in request setup: ${(error as Error).message}`);\n      }\n    }\n  },\n};\n",
    "taille": 13070
  },
  {
    "microservice": "mesx-leveling-board-ui",
    "chemin": "/src/store/filter-store.ts",
    "extension": ".ts",
    "prioritaire": false,
    "contenu": "import { defineStore } from 'pinia';\nimport { UserPreference } from '../modules/leveling-board/dto/userPreference';\nimport { FilterConfig } from '../modules/leveling-board/filter-config/dtos/filter-config.dto';\nimport {\n  createFilterConfig,\n  getFilterConfigByPlant,\n  updateFilterConfig,\n  getFilterByUserAndGroup,\n} from '../modules/leveling-board/filter-config/services/api';\n\nimport * as api from '../modules/leveling-board/service/api';\nimport { useProfileStore } from './user-auth-store';\n\nexport const useFilterStore = defineStore('filterStore', {\n  actions: {\n    applyUserPreferences(preferences: Array<UserPreference | FilterConfig>) {\n      for (const preference of preferences) {\n        if (preference.userCriteria) {\n          const criteria = preference.userCriteria.split(';');\n          for (const criterion of criteria) {\n            const [type, values] = criterion.split(':');\n            const valueArray = values ? values.split(',') : [];\n            switch (type) {\n              case 'materials': {\n                this.materialsSelected = valueArray;\n\n                break;\n              }\n              case 'tpa': {\n                this.tpaSelected = valueArray;\n                break;\n              }\n              case 'picking-order': {\n                this.pickingOrderSelected = valueArray;\n                break;\n              }\n              case 'work-center': {\n                this.workCentersSelected = valueArray.map((value) => {\n                  const [id, label] = value.split('-');\n                  const isParent = label.includes('parent');\n                  return {\n                    id: value,\n                    label,\n                    parentId: isParent ? undefined : id.slice(0, 6), // UAP part for parents, assuming format\n                    level: isParent ? 0 : 1,\n                  };\n                });\n                break;\n              }\n              case 'customer': {\n                this.customersSelected = valueArray;\n                break;\n              }\n              case 'srcLoc': {\n                this.srcTypSelected = valueArray;\n                break;\n              }\n              case 'destLoc': {\n                this.destTypSelected = valueArray;\n                break;\n              }\n              case 'custom-status': {\n                this.custStatusSelected = valueArray;\n                break;\n              }\n              case 'picking-status': {\n                this.pickingStatusSelected = valueArray;\n                break;\n              }\n              case 'outbound-del': {\n                this.outboundDeliverySelected = valueArray;\n                break;\n              }\n              // Apply the selected date from preferences\n              case 'date': {\n                this.selectedDate =\n                  values === 'CustomDateRange' ? 'CurrentDay' : values;\n                break;\n              }\n              case 'flow': {\n                this.flow = values === '1';\n                break;\n              }\n              case 'alert': {\n                this.alert = values === '1';\n                break;\n              }\n              case 'pikStatus': {\n                this.pikStatus = values === '1';\n                break;\n              }\n              case 'closedWKs': {\n                this.closedWKs = values !== '0';\n                break;\n              }\n              case 'start-preparation-type': {\n                this.startPreparationType = values;\n                break;\n              }\n              case 'start-preparation-time': {\n                this.startPreparationTime = values;\n                break;\n              }\n              case 'truck-departure-type': {\n                this.truckDepartureType = values;\n                break;\n              }\n              case 'truck-departure-time': {\n                this.truckDepartureDate = values;\n                break;\n              }\n              default: {\n                break;\n              }\n            }\n          }\n        }\n      }\n    },\n    async fetchUserPreferences(\n      groupName?: string,\n    ): Promise<Array<UserPreference | FilterConfig>> {\n      const userProfile = useProfileStore();\n      const { plant } = userProfile.userAttributes;\n      const userId = localStorage.getItem('userId') || 'undefined';\n\n      try {\n        if (!groupName) {\n          const preferences: Array<UserPreference> =\n            await api.fetchUserPreferenceByPlantUserIdAndAppName(\n              Number(plant),\n              Number(userId),\n              'LEVELING-BOARD',\n            );\n          this.applyUserPreferences(preferences);\n          return preferences;\n        }\n        const groupPreference: FilterConfig | null =\n          await getFilterByUserAndGroup(plant, groupName, 'LEVELING-BOARD');\n        this.selectedDropdownGroup = groupPreference.groupName;\n        const preferences: Array<FilterConfig> = [groupPreference];\n        this.applyUserPreferences(preferences);\n        return preferences;\n      } catch (error) {\n        console.error('Error fetching preferences:', error);\n        return [];\n      }\n    },\n    formatUserCriteria(): string {\n      const filterTypes = [\n        { type: 'materials', values: this.materialsSelected },\n        { type: 'tpa', values: this.tpaSelected },\n        {\n          type: 'picking-order',\n          values: this.pickingOrderSelected ? [this.pickingOrderSelected] : [],\n        },\n        {\n          type: 'work-center',\n          values: this.workCentersSelected,\n        },\n        { type: 'customer', values: this.customersSelected },\n        { type: 'srcLoc', values: this.srcTypSelected },\n        { type: 'destLoc', values: this.destTypSelected },\n        { type: 'custom-status', values: this.custStatusSelected },\n        { type: 'picking-status', values: this.pickingStatusSelected },\n        { type: 'outbound-del', values: this.outboundDeliverySelected },\n      ];\n\n      const toggleCriteria = [\n        { type: 'flow', value: this.flow },\n        { type: 'alert', value: this.alert },\n        { type: 'pikStatus', value: this.pikStatus },\n        { type: 'closedWKs', value: this.closedWKs },\n      ].map((toggle) => `${toggle.type}:${toggle.value ? '1' : '0'}`);\n\n      return [\n        ...filterTypes\n          .map((filter) => {\n            if (filter.type === 'work-center') {\n              const values = Array.isArray(filter.values)\n                ? filter.values\n                    .map((v) =>\n                      typeof v === 'object' && 'id' in v ? v.id : '',\n                    )\n                    .filter((id) => !!id)\n                : [];\n              return values.length > 0\n                ? `${filter.type}:${values.join(',')}`\n                : '';\n            }\n\n            const values = Array.isArray(filter.values)\n              ? filter.values.map(String)\n              : [];\n            return values.length > 0\n              ? `${filter.type}:${values.join(',')}`\n              : '';\n          })\n          .filter((criteria) => criteria !== ''),\n        ...toggleCriteria,\n        ...(this.selectedDate ? [`date:${this.selectedDate}`] : []),\n        ...(this.startPreparationType\n          ? [`start-preparation-type:${this.startPreparationType}`]\n          : []),\n        ...(this.startPreparationTime\n          ? [`start-preparation-time:${this.startPreparationTime}`]\n          : []),\n        ...(this.truckDepartureType\n          ? [`truck-departure-type:${this.truckDepartureType}`]\n          : []),\n        ...(this.truckDepartureDate\n          ? [`truck-departure-time:${this.truckDepartureDate}`]\n          : []),\n      ].join(';');\n    },\n    async saveFilterConfig(\n      layoutName: string,\n      groupName: string,\n      plant: string,\n      userCriteria: string,\n      userId: number,\n    ) {\n      if (!groupName) {\n        throw new Error('Layout name is required');\n      }\n      if (!userCriteria || Object.keys(userCriteria).length === 0) {\n        throw new Error('At least one filter value must be selected');\n      }\n\n      const payload = {\n        appName: layoutName,\n        groupName,\n        plant,\n        userCriteria,\n        userId,\n      };\n      return createFilterConfig(payload);\n    },\n    async saveFilterPreferences() {\n      this.displayFilter = false;\n      const userProfile = useProfileStore();\n      const plant = Number(userProfile.userAttributes.plant);\n      const userCriteria = this.formatUserCriteria();\n      const userId = localStorage.getItem('userId') ?? 'undefined';\n      const preferenceData: UserPreference = {\n        action: 'FILTER',\n        appName: 'LEVELING-BOARD',\n        plant,\n        user: { userId },\n        userCriteria,\n      };\n      try {\n        const result = await api.saveUserPreferences([preferenceData]);\n        return result;\n      } catch (error) {\n        console.error('Error saving preferences:', error);\n        throw error;\n      }\n    },\n\n    async fetchFilterConfigByPlant(plant: string, appName: string) {\n      try {\n        const filterConfigs = await getFilterConfigByPlant(plant, appName);\n        return filterConfigs;\n      } catch (error) {\n        console.error('Error fetching filter configs:', error);\n        return [];\n      }\n    },\n\n    async updateFilterConfig(id: number, data: FilterConfig) {\n      try {\n        const updated = await updateFilterConfig(id, data);\n        return updated;\n      } catch (error) {\n        console.error('Error updating filter config:', error);\n        throw error;\n      }\n    },\n  },\n\n  state: () => ({\n    alert: true as boolean,\n    closedWKs: false as boolean,\n    custStatusList: [] as Array<string>,\n    custStatusSelected: [] as Array<string>,\n    customStartPreparationDate: '' as string,\n    customTruckDepartureDate: '' as string,\n    customerList: [] as Array<string>,\n    customersSelected: [] as Array<string>,\n    destTypList: [] as Array<string>,\n    destTypSelected: [] as Array<string>,\n    displayFilter: false,\n    isDefault: true,\n    selectedDropdownGroup: '' as string | null,\n    flow: true as boolean,\n    materialsList: [] as Array<string>,\n    materialsSelected: [] as Array<string>,\n    options: ['All', 'Past', 'Specific', 'Future'],\n    outboundDeliveryList: [] as Array<string>,\n    outboundDeliverySelected: [] as Array<string>,\n    pickingOrder: '' as string,\n    pickingOrderList: [] as Array<string>,\n    pickingOrderSelected: [] as Array<string>,\n    pickingStatus: '',\n    pickingStatusList: ['IN PRODUCTION', 'CLOSED', 'TAKE FROM POOL'],\n    pickingStatusSelected: [] as Array<string>,\n    pikStatus: true as boolean,\n    preReschedule: true as boolean,\n    selectedDate: 'CurrentDay' as string, // Default to 'Current Day'\n    shopOrderSelected: [] as Array<string>,\n    showPopup: false,\n    sourceTypList: [] as Array<string>,\n    srcTypSelected: [] as Array<string>,\n    startPreparationLabel: 'ALL' as string,\n    startPreparationTime: '',\n    startPreparationType: '',\n    tpaList: [] as Array<string>,\n    tpaSelected: [] as Array<string>,\n    truckDepartureDate: '',\n    truckDepartureLabel: 'ALL' as string,\n    truckDepartureType: '',\n    workCentersSelected: [] as Array<{\n      id: string;\n      label: string;\n      parentId: string | undefined;\n      level: number;\n    }>,\n    workcenterList: [] as Array<{ wcCode: string; wcDesc: string }>,\n  }),\n});\n",
    "taille": 11352
  },
  {
    "microservice": "mesx-leveling-board-ui",
    "chemin": "/src/store/groups-store.ts",
    "extension": ".ts",
    "prioritaire": false,
    "contenu": "import { defineStore } from 'pinia';\n\ninterface Group {\n  id: string;\n  content: string;\n  visible: boolean;\n  wcDesc: string;\n  sourceType: string;\n  customerDesc: string;\n  nestedInGroup: string;\n}\n\nexport const useGroupStore = defineStore('groupStore', {\n  actions: {\n    closeDialog() {\n      this.dialogVisible = false;\n      this.stopAutoRefresh = false;\n    },\n\n    getGroups() {\n      return this.groups;\n    },\n\n    openDialog() {\n      this.dialogVisible = true;\n      this.stopAutoRefresh = true;\n    },\n\n    saveCurrentOrder() {\n      this.lastSavedGroups = [...this.groups];\n      this.stopAutoRefresh = false;\n    },\n\n    setGroups(newGroups: Array<Group>) {\n      this.groups = newGroups;\n    },\n\n    toggleDialog() {\n      this.dialogVisible = !this.dialogVisible;\n    },\n  },\n  state: () => ({\n    dialogVisible: false,\n    groups: [] as Array<Group>,\n    lastSavedGroups: [] as Array<Group>,\n    stopAutoRefresh: false,\n  }),\n});\n",
    "taille": 948
  },
  {
    "microservice": "mesx-leveling-board-ui",
    "chemin": "/src/store/user-auth-store.ts",
    "extension": ".ts",
    "prioritaire": false,
    "contenu": "import {\n  authorizeUser,\n  AuthConfigService,\n  ApiService,\n  UserAttributesManager,\n} from '@mesx/common-ui-lib';\nimport { WebStorageStateStore } from 'oidc-client';\nimport { defineStore } from 'pinia';\nimport { useRouter } from 'vue-router';\nimport { configuration } from '../Auth/configuration.ts';\nimport { getUser, User } from '../Auth/user-service.ts';\nimport { AuthConfigDTO } from '../modules/leveling-board/dto/auth.config.dto.ts';\nimport appLogger from '../utils/logger/app-logger.ts';\nimport { useUserStore } from './user-store.ts';\n\ninterface State {\n  profile: string;\n  user: User | undefined;\n  userAttributes: { [key: string]: string };\n}\n\nconst authconfig = new AuthConfigDTO(\n  configuration.getAuthority(),\n  configuration.getClientId(),\n  configuration.getRedirectUri(),\n  configuration.getResponseType(),\n  configuration.getScopes(),\n  new WebStorageStateStore({ store: window.sessionStorage }),\n  configuration.getAccessTokenExpiringNotificationTime(),\n);\nconst umeBaseUrl = import.meta.env.UME_API_URL;\nconst appName = 'LEVELING-BOARD';\nconst token = import.meta.env.TOKEN;\nconst redirectUri = import.meta.env.APP_REDIRECT_URI;\nlet appBasePath: string;\nif (redirectUri) {\n  const parts = redirectUri.split('/');\n  appBasePath = parts.pop();\n}\nconst authService = new AuthConfigService(authconfig);\nconst apiService = new ApiService(umeBaseUrl, token, appName);\nconst userAttributesManager = new UserAttributesManager(apiService);\n\nexport const useProfileStore = defineStore('user', {\n  actions: {\n    async authenticateUser() {\n      const useUser = useUserStore();\n      const user = await getUser();\n      if (!user) {\n        appLogger.error('User not found');\n        return;\n      }\n      if (user) {\n        const fetchedUser = await apiService.getUserByEmail(user.profile.email);\n        if (fetchedUser) {\n          localStorage.setItem('userId', fetchedUser.userId);\n          await apiService.updateUserLastLoginByEmail(\n            fetchedUser.email,\n            new Date(),\n          );\n        } else {\n          const userDetails = {\n            firstName: user.profile.given_name,\n            lastName: user.profile.family_name,\n            email: user.profile.email,\n            lastLogin: new Date(),\n          };\n          await apiService.saveUser(userDetails);\n        }\n        useUser.setProfile(\n          `${user.profile.given_name} ${user.profile.family_name}`,\n        );\n      }\n    },\n    async authorizeUser() {\n      const router = useRouter();\n      const user = await getUser();\n      if (user) {\n        const roleNamesString = user.profile.groups.join(',');\n        await authorizeUser(apiService, roleNamesString, router, appBasePath);\n      }\n    },\n    async fetchUserAttributes() {\n      try {\n        const attributes = await userAttributesManager.loadAttributes();\n        const userAttributes =\n          userAttributesManager.transformAttributesToStorageFormat(attributes);\n        if (userAttributes) {\n          this.userAttributes = { ...userAttributes };\n        } else {\n          appLogger.warn('No user attributes found.');\n        }\n      } catch (error) {\n        appLogger.error(`Error fetching user attributes: ${error}`);\n      }\n    },\n    async getUser(): Promise<User | undefined> {\n      if (this.user) {\n        return this.user;\n      }\n      const user = await getUser();\n      if (user) {\n        this.user = user;\n      }\n      return user;\n    },\n    async initializeAuthentication() {\n      const router = useRouter();\n      await authService.initializeAuthentication(router);\n    },\n    async isAuthenticated(): Promise<boolean> {\n      return authService.isAuthenticated();\n    },\n    async logout() {\n      try {\n        await authService.logout();\n        localStorage.removeItem('userAuth');\n        localStorage.removeItem('userAttributes');\n      } catch {\n        appLogger.error('Log out failed');\n      }\n    },\n  },\n\n  persist: {\n    key: 'userAttributes',\n    paths: ['userAttributes'],\n  },\n  state: (): State => ({\n    profile: '',\n    userAttributes: {} as { [key: string]: string },\n    user: undefined as User | undefined,\n  }),\n});\n",
    "taille": 4139
  },
  {
    "microservice": "mesx-leveling-board-ui",
    "chemin": "/src/store/user-store.ts",
    "extension": ".ts",
    "prioritaire": false,
    "contenu": "import { User, UserAttribute } from '@mesx/common-ui-lib';\nimport { defineStore } from 'pinia';\n\nexport const useUserStore = defineStore({\n  actions: {\n    setProfile(name: string) {\n      this.profile = name; // Save the profile name\n    },\n    setTitle(newTitle: string) {\n      this.title = newTitle;\n    },\n  },\n  id: 'userStore',\n  state: () => ({\n    profile: '',\n    title: '',\n    user: {} as User,\n    userAttributes: [] as Array<UserAttribute>,\n  }),\n});\n",
    "taille": 465
  },
  {
    "microservice": "mesx-leveling-board-ui",
    "chemin": "/src/types/notify.ts",
    "extension": ".ts",
    "prioritaire": false,
    "contenu": "export interface Notify {\n  success: (options: { title: string; message: string }) => void;\n  error: (options: { title: string; message: string }) => void;\n  warning: (options: { title: string; message: string }) => void;\n}\n",
    "taille": 224
  },
  {
    "microservice": "mesx-leveling-board-ui",
    "chemin": "/src/types/user-profile.dto.ts",
    "extension": ".ts",
    "prioritaire": false,
    "contenu": "export class UserProfile {\n  public email: string;\n\n  public given_name: string;\n\n  public family_name: string;\n\n  public groups: Array<string>;\n\n  public constructor(\n    email: string,\n    name: string,\n    family: string,\n    groups: Array<string>,\n  ) {\n    this.email = email;\n    this.given_name = name;\n    this.family_name = family;\n    this.groups = groups;\n  }\n}\n",
    "taille": 373
  },
  {
    "microservice": "mesx-leveling-board-ui",
    "chemin": "/src/utils/helpers/format-date.ts",
    "extension": ".ts",
    "prioritaire": false,
    "contenu": "/**\n *\n * @param  inNewPicking - The input date string to be formatted.\n * @returns The formatted date string in ISO 8601 format.\n */\nexport function formatDate(inNewPicking: string): string {\n  const myDate = new Date(Date.parse(inNewPicking));\n  const year = myDate.getFullYear();\n  const month = (myDate.getMonth() + 1).toString().padStart(2, '0');\n  const day = myDate.getDate().toString().padStart(2, '0');\n  const hours = myDate.getHours().toString().padStart(2, '0');\n  const minutes = myDate.getMinutes().toString().padStart(2, '0');\n  const iso8601String = `${year}-${month}-${day}T${hours}:${minutes}:00`;\n  return iso8601String;\n}\n",
    "taille": 642
  },
  {
    "microservice": "mesx-leveling-board-ui",
    "chemin": "/src/utils/logger/app-logger.ts",
    "extension": ".ts",
    "prioritaire": false,
    "contenu": "class AppLogger {\n  public log(message: unknown): void {\n    console.log(`${new Date().toISOString()} -- ${message}`);\n  }\n\n  public error(message: unknown, stack?: string): void {\n    console.error(`${new Date().toISOString()} -- ${message}`, stack);\n  }\n\n  public warn(message: unknown): void {\n    console.warn(`${new Date().toISOString()} -- ${message}`);\n  }\n\n  public debug(message: unknown): void {\n    console.debug(`${new Date().toISOString()} -- ${message}`);\n  }\n\n  public verbose(message: unknown): void {\n    console.info(`${new Date().toISOString()} -- ${message}`);\n  }\n}\nexport default new AppLogger();\n",
    "taille": 619
  },
  {
    "microservice": "mesx-leveling-board-ui",
    "chemin": "/src/vite-env.d.ts",
    "extension": ".ts",
    "prioritaire": false,
    "contenu": "/// <reference types=\"vite/client\" />\n",
    "taille": 38
  },
  {
    "microservice": "mesx-leveling-board-ui",
    "chemin": "/vite.config.ts",
    "extension": ".ts",
    "prioritaire": false,
    "contenu": "import { defineConfig } from 'vite'\nimport vue from '@vitejs/plugin-vue'\nimport { fileURLToPath, URL } from 'node:url'\nimport importMetaEnv from \"@import-meta-env/unplugin\";\nimport { quasar, transformAssetUrls } from '@quasar/vite-plugin'\n\n\nconst ASSET_URL = process.env.APP_REDIRECT_URI || '/lvl-ui';\n\n\nexport default defineConfig({\n  base: ASSET_URL,\n  plugins: [\n    vue({\n      template: { transformAssetUrls }\n\n    }),\n    importMetaEnv.vite({ example: \".env.dist\" }),\n    quasar()\n  ],\n  define: {\n    'process.env.NODE_ENV': JSON.stringify('production'),\n  },\n  resolve: {\n    alias: {\n      '@': fileURLToPath(new URL('./src', import.meta.url))\n    }\n  },\n\n  \n})\n",
    "taille": 671
  },
  {
    "microservice": "mesx-leveling-board-ui",
    "chemin": "/vue.config.js",
    "extension": ".js",
    "prioritaire": false,
    "contenu": "const { defineConfig } = require('@vue/cli-service')\nmodule.exports = defineConfig({\n  pluginOptions: {\n    quasar: {\n      importStrategy: 'kebab',\n      rtlSupport: false\n    }\n  },\n  transpileDependencies: [\n    'quasar'\n  ]\n})\n",
    "taille": 231
  }
]